# 实施记录：工程、基础能力与配置工作台阶段

**实施日期：** 2026-08-01  
**范围：** 任务 001—011、013—030 的框架交付；任务 012 保持待外部业务输入状态。

## 任务状态

| 任务 | 状态 | 实现与验证证据 | 未解决风险 |
| --- | --- | --- | --- |
| 001 外部契约澄清 | 已完成 | `外部契约清单.md` 记录状态、责任人、目标日期、影响任务和当前处理。 | 责任人、日期及所有外部输入待项目方确认。 |
| 002 独立工程初始化 | 已完成 | `package.json` 提供开发、构建、类型检查和单元测试命令；构建输出到 `dist`。 | 生产部署服务器回退策略待部署方确认。 |
| 003 业务域目录与依赖边界 | 已完成 | `src` 按应用、布局、模块、共享、服务、状态和工具划分；`目录与依赖规则.md` 明确禁止依赖。 | 后续新增模块需在代码评审中持续执行规则。 |
| 004 设计令牌与全局样式 | 已完成 | `styles/tokens.css` 提供颜色、间距、层级和暗色模式；全局样式只使用令牌。 | 具体品牌视觉规范未提供。 |
| 005 应用壳与三类布局 | 已完成 | 门户、后台、可视化布局以及全局错误边界均已接入路由。 | 网页图形单实例宿主依赖任务 031。 |
| 006 一级导航与模块入口 | 已完成 | 五个模块入口由 `config/navigation.ts` 统一驱动并按页面权限过滤。 | 正式权限码待鉴权契约确认。 |
| 007 路由懒加载与可视化路由 | 已完成 | 总览、工艺、专题入口和专题详情路由均为页面级动态导入；工艺页共用一个组件。 | 生产服务器回退策略仍待部署方确认。 |
| 008 三层权限策略模型 | 已完成 | `permission.store.ts` 分离页面、数据和场景权限，并有单元测试。 | 当前为模拟策略，不能替代正式鉴权。 |
| 009 工艺页路由守卫 | 已完成 | 先校验工艺/专题配置存在性，再检查配置权限码；未知标识进入 404，不会生成资源地址。 | 正式权限策略仍待鉴权契约替换。 |
| 010 统一加载、降级与异常状态 | 已完成 | `AppStatePanel` 覆盖加载、握手、三维不可用、数据延迟、无权限、配置缺失六类状态。 | 后续接入时需补充真实错误码到原因映射。 |
| 011 普通后台组件基座 | 已完成 | 表格、表单字段、查询区、抽屉、图表容器与示例页面可运行。 | 业务字段未确认，禁止直接扩展为业务增删改查。 |
| 012 普通业务模块需求澄清 | 未开始 | 入口页面显式展示待确认项，未伪造字段和流程。 | 等待业务负责人提供字段字典、状态流转、权限与接口。 |
| 013 稳定业务 ID 与运行时校验 | 已完成 | `config/process/identifiers.ts` 拒绝中文标题、Unity 层级路径和图片/模型文件名；页面、步骤、节点、路由、指标和运行时键均为区分类型。 | 正式配置服务接入时需复用同一校验器。 |
| 014 工艺配置模型 | 已完成 | 页面、域导航、导览、拓扑、详情、数据绑定、场景映射和专题均以同版本纯数据模型定义。 | 真实数据字段和数据权限待外部契约确认。 |
| 015 网页图形运行时登记 | 已完成 | 只读运行时登记表隔离入口地址、精确来源、协议能力、资源预算、缓存和回滚字段；本地未登记正式地址。 | 需由部署方提供正式入口、父页面来源和资源版本。 |
| 016 配置原子加载 | 已完成 | 加载器仅查询当前页面，汇集页面、拓扑、导览、详情、场景映射和运行时后统一校验，并按页面版本缓存。 | 远程配置服务接入后需补充缓存失效通知。 |
| 017 配置发布校验 | 已完成 | 校验版本、ID、拓扑引用、导览引用、详情引用、场景映射和运行时安全边界；错误时网页图形降级。 | 发布平台侧尚未接入同一校验器。 |
| 018 配置化导航与权限 | 已完成 | 9 个工艺域、34 个页面由配置驱动；导航隐藏未授权页，路由仍执行权限校验。 | 正式账号权限码待鉴权契约替换。 |
| 019 静态预览与结构化空态 | 已完成 | 正式网页图形运行时未登记时，燃气页显示静态预览；未发布页面显示结构化场景不可用态，无 iframe。 | 正式静态图片资源待设计或业务方提供。 |
| 020 统一工艺工作台 | 已完成 | 共用页面组件与响应式三栏布局已交付：工艺导航、场景/拓扑、导览/详情。 | 真实 WebGL 宿主将在任务 031 后接入。 |
| 021 工艺上下文状态仓库 | 已完成 | Pinia 仅保存可序列化页面、选中、能力、命令和指标快照；Canvas/WebGL 对象不进入状态。 | 实时指标契约未就绪，当前只保留空快照能力。 |
| 022 工艺动作协调器 | 进行中 | 导览、拓扑与未来 WebGL 事件共用关联标识和有界幂等缓存；WebGL 来源不会产生回写命令。 | 需补齐拓扑选中描边、三维聚焦描边与失败降级的统一动作。 |
| 023 场景面板降级 | 已完成 | 未登记运行时安全降级至静态预览；声明网页图形（WebGL）能力的页面固定预留 320 像素场景视口（窄屏 300 像素），静态预览与正式三维共用空间；空模式页面仍按内容展示场景不可用状态。 | 正式运行时连接、超时和重试策略待任务 031。 |
| 024 工艺导览 | 已完成 | 燃气总览提供 8 个步骤，并明确标记未确认路由不会播放三维流动效果。 | 双机组单独控制与热点规则待业务确认。 |
| 025 设备详情块 | 已完成 | 详情块按页面/数据权限及数据可用性过滤；未接实时数据契约时不生成模拟指标。 | 设备字段、告警和运维数据待外部契约。 |
| 026 二维拓扑模型 | 进行中 | 节点、连线、证据状态、分层边界、受控图元键、四态状态与已确认场景路由均由配置表达；燃气总览按企业 IT、生产 DMZ、厂级、单元和现场设备五层发布，未接入状态契约的节点明确为离线。 | 需登记实时状态时间、完成状态增量归一与稳定三维映射。 |
| 027 拓扑渲染器抽象 | 进行中 | `TopologyRenderer` 接口隔离工作台与具体绘制技术；选择态与设备状态保持独立。 | 需补齐场景状态联动抽象。 |
| 028 Canvas 拓扑适配器 | 进行中 | 单实例 Canvas 适配器支持固定 270 像素拓扑视口、缩放、鼠标左键拖拽平移、图例、节点点击、选中描边、正交箭头、协议标注和高像素比限制；节点从受控登记表加载四态 Cisco 风格 SVG，选中描边不覆盖状态图元。 | 需接入实时状态增量消息，并补齐各状态与三维同步回归。 |
| 029 拓扑性能控制 | 进行中 | 静态索引、命中缓存、动画帧合并、像素比上限、标签收缩和卸载释放均已实现。 | 需按节点增量同步四态状态至场景联动层，并以实际节点量补充性能基线。 |
| 030 总览与专题配置 | 已完成 | 可视化总览统计配置数量；5 个专题由独立配置、权限和路由守卫承载，并显示资源未发布状态。 | 专题具体页面和资源待对应外部契约。 |

## 拓扑图元规则增补

2026-08-01 新增“拟物化 Cisco 风格图元与四态设备状态”规则后，任务 026—029 重新进入实施：现有拓扑模型、渲染器、画布适配器和性能策略尚未登记图元键，也未支持正常、告警、故障、离线状态的增量切换。任务 022 同时重新进入实施，用于补齐二维选中描边、三维聚焦描边与失败降级的单一选择动作。该调整已同步到任务总览，待完成受控图元登记、状态契约、渲染替换和测试后再恢复完成状态。

## 二维拓扑—三维场景联动规则增补

2026-08-01 已新增二维拓扑与三维场景联动规范。任务 031、039、040、042 因此重新进入实施：既有网页图形协议、Unity 桥接和本地回归夹具保留，但需补齐 `selectionId`、`setNodeVisualState`、模型聚焦描边、四态材质效果和回归用例。任务 047、051、090—092 需在实时数据接入与燃气闭环阶段验证状态增量同步、选择无回环和端到端视觉一致性。

## 自动化验证

| 检查项 | 结果 |
| --- | --- |
| 类型检查 | 通过：`npm run typecheck`。 |
| 单元测试 | 通过：5 个测试文件、11 个测试用例。 |
| 生产构建 | 通过：`npm run build`，页面按路由生成独立代码块。 |
| 开发模式 | 通过：本地开发服务器可启动并完成页面回归。 |
| 页面验证 | 燃气工作台显示静态预览、二维拓扑、8 步导览与详情联动；桌面与 1024 宽度均无横向溢出；Canvas 数为 1、iframe 数为 0、控制台无错误。 |

## 手工验证记录

1. 访问 `/portal`，确认系统门户、数据采集、数据管理、可视化、系统管理五个入口均渲染。
2. 访问 `/visual/process/gas-overview`，确认页面显示“静态预览”、二维工艺拓扑、9 域导航和 8 步导览；未创建 iframe。
3. 点击“燃气轮机”导览步骤，确认步骤高亮、二维拓扑高亮和“燃气轮机”详情标题同步更新。
4. 在 1024 像素宽度下确认工作台无横向溢出，右侧导览/详情按响应式规则移至下方。

## 变更范围

- 工程配置：根目录构建、类型检查和测试配置。
- 应用与样式：`src/App.vue`、`src/app`、`src/styles`、`src/layouts`、`src/router`、`src/config`、`src/stores`。
- 页面与组件：`src/modules`、`src/shared/components`、`src/modules/visual/components`。
- 基础设施与测试：`src/services`、`src/services/topology`、`src/modules/visual/services`、`src/shared/utils`、对应单元测试。
- 交付文档：`README.md`、`docs/外部契约清单.md`、`docs/目录与依赖规则.md`、本记录。
