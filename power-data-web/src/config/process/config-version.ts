/**
 * 本机燃气、燃煤工艺配置的唯一发布版本。
 *
 * 页面、拓扑、导览、详情、场景映射与网页图形运行时登记表都必须引用本常量。
 * 该值还会作为 iframe（内嵌框架）地址参数传给 Unity；它必须与发布结构清单的
 * sceneMappingVersion（场景映射版本）完全一致。若两处独立手工维护，Unity 会按协议
 * 拒绝 switchScene（场景切换）命令，导致用户无法从全局沙盘进入燃气或燃煤场景。
 *
 * 当前版本对应燃煤、燃气分层映射目录。节点命名规范化属于此映射目录的内容变更，
 * 不能另设前端运行时版本，以免握手版本与切场景版本发生漂移。
 */
export const LOCAL_PROCESS_CONFIG_VERSION = '2026.09.10-coal-layer-split.1' as const
