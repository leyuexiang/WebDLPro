# 任务-052：三层通信端到端测试实施状态

> **2026-08-15 协议纠偏：** 旧平台运行时清单和设备编号夹具只保留为历史证据。任务-056已完成本地节点协议迁移；新的三层验收必须覆盖平台读取结构清单、平台内部绑定 `nodeId`、双击收到 `nodeId`、状态按 `nodeId` 更新以及三维失败不影响回执。合作方实测未完成，任务进度保持65%。

**状态：** 进行中（65%）
**更新时间：** 2026-08-15

## 2026-08-14 燃气视觉联动基线边界

`Builds/Releases/燃气发电场景与拓扑图联动包0813` 现作为三层联调中的燃气视觉与交互标准：真实燃气总场景、23/22总拓扑、三个关键环节过滤视图、三维聚焦描边、空白清除交互描边、原生全屏及总览复位均不得在后续状态对接中回退。该包没有当前节点协议和真实四态状态，因此只能作为后续端到端测试的稳定前置夹具，不能关闭结构清单读取、节点事件、完整状态快照和真实模型状态验收。任务完成度保持65%。

## 2026-08-14 清单请求缓存边界补强

- `RemoteSceneTopologyManifestLoader`（远程场景拓扑清单读取器）在请求我方结构清单时明确发送 `cache: no-store`（禁止浏览器缓存），同时继续使用 `credentials: omit`（不携带凭据）和有限超时、外部取消。
- 读取器强制检查响应的 `Cache-Control`（缓存控制）包含 `no-cache`（禁止缓存）令牌；缺失或错误时以固定 `manifest.cache-policy` 失败码停止初始化，不把可能过期的结构清单交给运行时。
- 对 HTTP 404（未找到）响应，读取器只识别合作方约定的 `package not found`（联动包不存在）和 `manifest file missing`（清单文件缺失）两个固定标识，并分别转换为稳定失败码；未知正文仍回退通用HTTP错误，不泄露服务端原文。
- 该设置只防止浏览器复用旧结构响应，不改变结构清单地址、响应格式或外层通信协议；服务端仍必须返回约定的 `Cache-Control: no-cache`（禁止缓存）响应头。
- 远程清单读取器专项测试、当轮前端全量45个测试文件268项、类型检查、生产构建和结构清单校验均通过。该证据只提升清单重载的本地安全边界，不提高任务65%，因为真实平台读取和真实状态链路仍未验收。
- 同轮内部验证包浏览器回归通过：拓扑原生全屏进入/退出、三个关键环节、返回23/22总览、单Unity实例和单拓扑画布均保持稳定，控制台及业务资源请求错误为0；合作方平台各层全屏权限仍未形成正式证据。
- 同轮新增独立端口结构清单合同夹具，验证有效清单和两类404固定响应；它只覆盖本地服务与读取器，不证明平台内部绑定或真实状态链路，任务继续保持65%。
- 合同夹具现由正式读取器实际访问三个接口，确认有效结构清单和两类404稳定失败码贯通；未把该本地服务响应当作合作方平台证据。

## 2026-08-14 本地完整快照链路补充

- 内置浏览器历史回归已通过“本地外层宿主页 → 当前嵌入壳 → Unity 模拟页”的连续完整快照链路。当前夹具已迁移为结构清单和两枚合成节点，仅用于受控链路验证，不代表合作方真实接口。
- 第一批完整表同时下发两台告警；第二批使用更小的来源修订号，仅保留燃机故障。两批二维 `command.result: completed`（命令完成）各返回一次；缺失发电机恢复二维基线，三维模拟页收到一次 `clearNodeVisualState`（清除节点视觉状态），最终只保留燃机故障动态覆盖。
- 正式协议入口对 `items: []` 继续返回载荷非法；空资源由结构清单表达，正常状态通道不发送空数组。
- 同轮基础三层回归通过查询关联、重复标识静默丢弃、旧会话/伪造来源拒绝和释放确认。该结果仅提升本地链路证据，不提高任务 `65%`，因为合作方真实结构清单读取、平台内部绑定、真实燃气模型四态和三维失败仍未端到端验收。
- 同一浏览器页在释放后重建测试夹具并再次执行完整快照：新会话重新建立三层握手，两批状态命令各只收到一次二维成功结果，最终诊断仍为设置 `3` 次、清除 `1` 次和仅燃机故障。重建仅通过夹具私有代次参数强制 iframe 重新导航，不变更正式实例、会话、来源或生产协议。

## 合作方契约变更复核

- 本地节点状态用例已验证连续完整快照整体替换、缺失节点二维基线恢复、三维清除、较小来源修订仍覆盖和三维失败隔离；但它使用固定合成清单，尚不能覆盖合作方真实网络失败或真实燃气模型四态。
- 新端到端夹具必须读取我方结构清单，并证明平台只在自身系统维护 `nodeId → 真实设备编号`，不改写 `doubleClickBehavior`、`sceneNodeId` 或其他结构字段。
- 外层成功回执必须在二维快照提交后形成；三维命令可继续异步执行。Unity 未就绪、能力缺失、部分失败或超时均不得改变该回执。
- 因合作方真实结构清单读取、平台内部绑定、跨场景快照和真实三维状态均属于端到端主链路，当前进度为65%。本地协议已补齐本地快照序号、三维失败隔离、显式不可恢复失败重试抑制和结构版本4构建，但尚未形成平台节点状态到真实模型四态的完整浏览器证据。
- 内置浏览器已在本地三层夹具复测：正常状态重试仍完成“首轮确认丢失、五秒跳过、十秒截止、最新快照重读、新标识成功回执”；另一轮在首条状态命令在途时发送释放，外层收到释放回执后超过十秒没有第二次发送，测试页以正常取消结束且控制台无业务错误。该证据只说明本地父页面遵循协议，不代表合作方平台网络、设备数据或动态绑定。

## 本轮浏览器验收证据

测试链路固定为“本地外层宿主页 → 当前嵌入壳 → Unity 模拟页”。外层宿主页不直连 Unity（网页图形运行时），每一项只从外层事件、命令结果和有限状态快照判定，避免直接读取子框架内部状态而绕过真实通信边界。

| 验收项 | 浏览器实际结果 | 结论 |
| --- | --- | --- |
| 初始化 | 收到 `system.ready`（系统就绪）后发送 `system.init`（系统初始化），获得关联 `system.ack`（系统确认）与燃气场景的初始视图变更 | 通过 |
| 同场景动作 | 燃气合成流程动作完成，保持同一业务场景并提交目标拓扑 | 通过 |
| 跨场景动作 | 合成燃气到风电流程完成，先切换场景、再执行动作并提交目标拓扑 | 通过 |
| 拓扑双击 | 自动双击实际画布命中测试节点，外层日志收到唯一 `topology.node.dblclick`（拓扑节点双击）及 `node.gas-turbine` | 通过 |
| 三维选择 | 点击 Unity 模拟对象后，外层收到映射节点 `node.gas-turbine` 并同步一个二维节点；验证未产生 `focusNode`（聚焦节点）回写 | 通过 |
| 节点状态 | 当前本地夹具已用两个合成节点验证两批非空完整快照整体替换、较小来源修订仍覆盖、缺失节点二维基线恢复和三维清除；固定清单不代表合作方真实节点状态，也未覆盖真实燃气模型四态与平台网络异常 | 本地通过，平台待验收 |
| 快速切换与异常恢复 | 连续三次请求仅最后一次提交；最终失败恢复、十秒超时与迟到回调隔离均通过 | 通过 |
| 错误来源与释放 | 基础回归确认旧会话和伪造来源被拒绝；释放命令收到关联确认且后续旧回调不再提交 | 通过 |

### 2026-08-15 当前节点协议浏览器复验

- 本地三层夹具已通过节点双击、三维反向选择、连续完整快照、状态边界、基础通信和单在途重试；外层只看到 `nodeId`，不含 `deviceId`。
- 测试宿主已显式授予协议壳内嵌框架原生全屏权限。三维和拓扑全屏均可进入、退出，且Unity内嵌框架始终为1个。
- 当前源码内部验证包 `Builds/Releases/task056-nodeid-browser-5554-20260815-0255` 已在内置浏览器显示真实燃气模型，并通过23/22总览、16/13、13/6、15/10三个关键流程、返回总览、两类原生全屏和单实例复用。
- 现有发布浏览器脚本补充确认最外层原生全屏提升、1个Unity内嵌框架、1个拓扑画布、3个关键流程、23/22总览恢复、业务资源错误0和控制台错误0。
- 上述结果仍是我方独立本地服务证据；合作方读取结构清单、平台内部绑定、真实节点状态和真实模型四态未验收，因此任务-052保持65%。

## 真实燃气网页图形联调包证据

### 2026-08-13 历史测试包（不作为当前协议基线）

历史独立发布目录为 `Builds/Releases/gas-power-smoke-ccgt-5539-task061-20260813-0042`，使用正式Unity网页图形 `task061-webgl-switch-ack-fixed-20260813-0036`。该包只记录当日燃气视觉联动验证：宿主页只初始化燃气场景，其余八个业务场景虽然保留空资源包目录，但不会由本测试页加载。当前状态协议基线为 `task038-state-clear-v4-20260813-232422`，当前平台联调包为 `gas-power-platform-task038-v4-5551-20260813-2330`；二者的当前事实见本文件第98行的结构版本4说明。

| 验收项 | 历史实际结果 | 结论 |
| --- | --- | --- |
| 正式协议构建 | 结构版本3；完整声明11项下行命令能力，场景完成含物理激活标识，切换含 `forceReload`，失败恢复含恢复激活标识 | 通过 |
| 燃气模型与总图 | 应用内浏览器真实显示现有燃气电厂模型；默认显示CCGT运营技术总图23节点、22连线 | 通过 |
| 三个外部动作 | `workflow.trigger` 依次切换燃气轮机、余热锅炉、蒸汽轮机关键环节，分别呈现16节点/13连线、13节点/6连线、15节点/10连线 | 通过 |
| 过滤与排布 | 三个关键环节均从总图显式过滤，无关节点和连线隐藏；`nodeLayoutOverrides` 由总图按节点标识投影，不维护独立拉伸坐标 | 通过 |
| 三维动作 | 三个动作均返回“三维聚焦已按动作结果提交”；蒸汽轮机截图确认模型聚焦及描边 | 通过 |
| 单实例 | 初始化及三个动作全过程始终只有一个Unity内嵌框架 | 通过 |
| 自动回归 | 最新发布契约锁定总图23/22、燃气轮机16/13、余热锅炉13/6、蒸汽轮机15/10（节点/连线） | 通过 |

### 2026-08-13 拓扑原生全屏补充包

发布目录 `Builds/Releases/gas-power-smoke-ccgt-native-topology-fullscreen-5540-20260813` 复用同一正式Unity网页图形，只更新前端壳的拓扑全屏实现。自动回归以真实用户点击进入全屏，并断言壳文档的 `document.fullscreenElement` 正是拓扑面板；退出后全屏元素为空，Unity内嵌框架数量仍为1，随后三个流程7/6/6节点回归继续通过，控制台和业务资源请求均无错误。该证据证明子应用已具备与三维相同的原生全屏机制；合作方实际平台仍必须为每层内嵌框架授予全屏权限。

### 2026-08-13 外部返回总拓扑补充包

发布目录 `Builds/Releases/gas-power-smoke-ccgt-overview-restore-5554-20260813` 增加公开 `view.open` 返回总览入口。浏览器按“总图23节点 → 燃气轮机7节点 → 余热锅炉6节点 → 蒸汽轮机6节点 → 总图23节点”执行；发布清单确认总图为未过滤的23节点、22连线定义，返回后总图标题恢复，拓扑画布和Unity内嵌框架均保持1个，控制台与业务资源错误均为0。该命令当前 `actionId` 为 `null`，因此只验收二维总拓扑恢复，不声称三维镜头和描边已经复位。

### 2026-08-13 三维与二维总览原子复位补充包

根因确认旧 `5541` 包的返回按钮发送 `view.open` 且 `actionId: null`，清单、场景动作白名单和Unity流程白名单均未登记总览动作；事务因此只恢复二维，不会调用Unity。新发布目录 `Builds/Releases/gas-power-smoke-overview-action-final-5543-20260813` 新增稳定动作 `action.gas-power.overview`，并通过 `workflow.trigger` 映射到 `enterProcessStep(gas-power-generation, overview, all, true)`。动作失败策略为 `keep-current-context`（保留当前上下文），因此Unity失败时不会先提交二维总图。

最新内置浏览器回归按“总图23节点/22连线 → 燃气轮机16/13 → 余热锅炉13/6 → 蒸汽轮机15/10 → 总览23/22”执行并通过；结构版本3清单包含总览及三个流程动作，Unity内嵌框架和拓扑画布保持单实例。真实模型进一步验证拓扑节点聚焦描边，以及空白点击只清除二维选择和三维交互描边而不复位镜头。截图为 `artifacts/browser-tests/task064-gas-overview-5544-20260813.png`、`artifacts/browser-tests/task064-gas-node-focus-5544-20260813.png` 和 `artifacts/browser-tests/task064-gas-clear-selection-verified-5544-20260813.png`。前端全量40个测试文件、230项及类型检查通过。任务仍保持65%，因为动态运行时清单、设备状态容量与三维失败隔离等主链路尚未完成。

**2026-08-13 旧包误用复核：** 用户复现地址 `http://127.0.0.1:5541/` 对应 `gas-power-smoke-task063-overall-20260813-5541` 历史发布。其宿主页返回按钮明确发送 `view.open`，载荷为 `actionId: null`，所以只能恢复二维总拓扑；这与Unity编辑器内 `overview` 步骤有效并不矛盾，因为该页面从未调用该步骤。随后对结构版本3正式包 `http://127.0.0.1:5544/` 重新执行完整三层自动回归，三个关键环节后返回总览成功，页面收到关联 `action.gas-power.overview` 的稳定视图事件，Unity恢复整厂总览、拓扑恢复23节点/22连线，Unity内嵌框架和拓扑画布均保持1个，控制台错误和业务资源请求错误均为0。复核截图为 `power-data-web/artifacts/gas-power-overview-5544-regression.png`。本次只纠正测试包版本并补充证据，不新增任务能力，因此任务进度保持65%。

### 2026-08-13 紧凑直线路由最终包

不可变发布目录 `Builds/Releases/gas-power-smoke-topology-compact-straight-final-5548-20260813-191145` 复用结构版本3 Unity基线 `task064-clear-selection-20260813-1610`，未重新构建Unity且未覆盖5544旧包。前端按画布受限维度响应式缩小图标、标题、层级文字、协议文字和选中框，独立保留至少40×40像素命中区；总图五层纵坐标固定为 `8、28、50、69、88`，三个关键环节直接投影总图坐标与真实层级。连线改为任意角度直线优先，遇到完整节点边界、共线重叠或非端点交叉时才最少绕行，协议标签沿斜线法线放置并保持水平。

自动端到端回归与内置浏览器共同验证：常规页面、拓扑原生全屏、总览与三个关键环节切换、返回总览、三维聚焦描边、空白清除二维选择和三维交互描边、单画布及单Unity实例全部通过；控制台与业务资源错误为0。实际覆盖 `500×108`、`677×188`、`1242×582` 全屏画布及 `3440×1440` 带鱼屏。前端40个测试文件、241项、类型检查、生产构建和发布清单校验全部通过。本轮没有新增平台动态清单或设备状态主链证据，任务进度保持65%。

截图证据：`artifacts/ccgt-task061-steam-turbine-browser-20260813.png`。独立浏览器自动回归的唯一历史404为旧固化包自动请求 `/favicon.ico`，已确认不涉及Unity、清单、拓扑、脚本、样式或图元；后续构建模板已使用空数据站点图标消除此噪声，测试也会对其他请求失败直接报错。

### 2026-08-13 平台入口与内部自测入口隔离

构建器默认生成平台包：根路径 `/`（`index.html`）只显示嵌入壳并自动初始化燃气场景和总拓扑，目录内不生成 `self-test.html`。只有显式传入内部自测开关时，才会额外生成包含四个流程按钮、状态文字和动作脚本的自测页。最终平台交付目录为 `Builds/Releases/燃气发电场景与拓扑图联动包0813`，包内发布标识为 `gas-power-platform-release-5550-20260813-2015`；目录检查确认无自测文件和测试文字，内置浏览器确认真实燃气模型与23/22总拓扑正常。内部验证包 `gas-power-platform-clean-topology-5549-20260813-2005` 完成三个关键流程、原生全屏、返回总览、单Unity实例和单画布自动回归，控制台及业务资源错误为0。该调整不改变三层消息通道、清单动作、Unity映射和节点数量；任务-052仍保持65%，因为设备动态清单、完整状态快照和三维失败隔离尚未形成端到端证据。

### 2026-08-14 状态协议构建与交付入口复核

当前 Unity 基线为 `task038-state-clear-v4-20260813-232422`，其元数据结构版本4声明 `setNodeVisualState` 结构版本3、`clearNodeVisualState` 结构版本1及本地 `snapshotSequence`。平台交付包为 `gas-power-platform-task038-v4-5551-20260813-2330`，仍按交付边界不包含 `self-test.html`；因此将四流程自测脚本直接指向 `http://127.0.0.1:5551/self-test.html` 得到404是预期的入口隔离结果，不能记为业务回归失败。平台包只验证根入口初始化、真实燃气模型、23/22总图、单Unity实例、单画布和联调协议；四流程自动回归继续只能使用明确包含自测页的内部验证包。节点状态专项3个文件29项通过，证明本地协议和构建门禁一致，但不替代平台动态绑定与真实模型状态端到端验收。

以下2026-08-05内容保留为历史协议基线，不再代表当前测试包：

本轮生成独立联调发布目录 `Builds/Releases/gas-power-smoke-task037-sceneid-20260805-1820`，并使用同目录 `server.mjs`（静态服务脚本）在 `http://127.0.0.1:5523/` 启动。外层宿主页仅在收到嵌入壳 `system.ready`（系统就绪）后发送 `system.init`（系统初始化），目标固定为 `gas-power`（燃气发电）与 `topology.gas-power.overview`（燃气总览拓扑）；宿主页不直接访问 Unity iframe（内嵌框架）。

| 验收项 | 实际结果 | 结论 |
| --- | --- | --- |
| Unity 正式基线 | 以 `gas-power-smoke-unity-20260805-144625` 正式网页图形构建为来源；场景资源目录仍含独立九场景资源包，宿主页只初始化燃气发电 | 通过 |
| 燃气模型加载 | 内部浏览器实际呈现现有燃气电厂三维模型，外层状态显示“燃气发电场景与对应拓扑已完成联调初始化” | 通过 |
| 燃气拓扑加载 | 当时直接投影 `gas-overview` 已校验配置，呈现 24 个节点、23 条连线；所有节点维持原配置的离线基线 | 通过 |
| 三层通信 | 外层宿主页 → 嵌入壳 → 真实 Unity 已完成初始化与稳定视图提交；没有由宿主页直接调用 Unity 的旁路 | 通过 |
| 16:9 与溢出 | Unity 画布实际为 740×416，宽高比 1.7788；内层文档未出现横向或纵向滚动条 | 通过 |
| Unity 网页模板 | 修复项目仍选择默认网页模板的问题，正式输出改用 `PROJECT:EmbeddedViewport`（项目嵌入式视口模板）；不再输出固定 960×600 画布或默认页脚 | 通过 |
| 人工正向联动 | 用户在最终联调包中点击拓扑节点，Unity 真实燃气模型聚焦并显示描边；该操作未通过宿主页直连 Unity | 通过 |

浏览器自动化环境在页面切换时记录了一条 `MutationObserver`（变更观察器）参数错误。项目源码没有 `MutationObserver` 调用，错误发生在自动化页面观察层，不来自前端壳、Unity 模板或 Unity 运行时；同一轮仍成功读取实际画布尺寸、真实模型、拓扑画布和外层稳定状态，因此不作为本包运行失败。

**2026-08-09 历史协议复核：** 当时的 `task051-final-20260805-2220` 不包含结构版本2元数据，旧包不能替代当前验收。随后 `task061`、`task064` 解除场景和清除交互描边构建缺口；当前节点状态协议以结构版本4的 `task038-state-clear-v4-20260813-232422` 为准，旧包不再作为设备状态链路证据。

**2026-08-13 清除描边证据边界：** 当日源码协议和三层本地夹具已验证先执行 `focusNode` 再独立执行 `clearSelection`，两条命令均成功回执，且清除命令不调用 `resetScene`。结构版本3正式Unity网页图形 `task064-clear-selection-20260813-1610` 已进入真实燃气联调包；内置浏览器确认描边消失且场景、流程、显隐和镜头不变。当日包没有设备状态输入，故该历史证据不覆盖告警描边与交互描边并存时的隔离，也不因此提前上调进度。

**2026-08-05 历史自动验证：** 当时前端39个测试文件、205项通过，类型检查和转换95个模块的生产构建通过；Unity编辑器模式22 / 22、播放模式9 / 9通过。该批证据只证明当时的源码与引擎内运行机制，不能替代上方2026-08-13正式网页图形、燃气总图和三流程真实浏览器证据；当前播放模式口径为11 / 11。

**2026-08-05 发布映射复核：** 新联调包 `Builds/Releases/gas-power-smoke-process-contract-5526-20260805-1945` 复用主播放器—资源目录发布标识精确匹配的 Unity 基线。其清单契约报告为 0 项问题，燃气映射为 6 个已核验三维节点、7 项已验证流程步骤、0 条三维路径，且明确不发布 `gas-network`。内部浏览器实际显示 24 个离线拓扑节点、一个 Unity 内嵌框架及“燃气发电场景与对应拓扑已完成联调初始化”；该结果只证明初始化和发布一致性，不替代用户已经完成的真实节点点击聚焦/描边人工验收。

## 分辨率与容器复核

三维容器不采用 `1280×720`（固定像素尺寸）上限。其高度由当前嵌入壳宽度的 16:9 换算值和动态视口可用高度共同决定；二维拓扑使用网格第二行的全部剩余空间。下列浏览器实测均保持 16:9，且没有外层纵向滚动条。

**2026-08-05 布局规则补充：** 燃气真实联调中发现宽屏 16:9 三维区会将五层拓扑压缩至不可读高度。现将规则修订为：先为拓扑保留响应式最小可读高度，再在其余高度内计算 16:9 三维视口；拓扑内层与 Unity 实际视口使用同一自适应宽度并居中。两侧未占用区域统一使用三维纯黑基底，不扩展拓扑青色背景。该规则不设置三维固定像素上限，也不改变外层无滚动条、单一 Unity 实例和单一拓扑画布约束。

**2026-08-05 全屏复核：** 发布包 `gas-power-smoke-20260805-152956` 中，拓扑全屏已解除常规态“与 Unity 同宽”的限制，改由子应用当前视口的 12 像素响应式安全边距确定尺寸。在 `643×920` 的嵌入视口实测中，常规态拓扑面板为 `643×557.3125`，全屏面板为 `619×896`，二维画布为 `593×771`；画布完整占据标题和状态摘要之外的剩余高度，右侧不再残留未被全屏覆盖的 Unity 区域。

| 请求浏览器视口 | 三维实际尺寸 | 二维拓扑高度 | 三维宽高比 | 外层纵向滚动条 |
| --- | ---: | ---: | ---: | --- |
| 2560×1440（2K） | 1485×835 | 604 | 1.7778 | 无 |
| 3840×2160（4K） | 2227×1253 | 906 | 1.7778 | 无 |
| 7680×4320（8K 请求） | 4096×2304 | 1791 | 1.7778 | 无 |
| 3440×1440（带鱼屏） | 1485×835 | 604 | 1.7778 | 无 |

## 未关闭项

1. 当前为受控 Unity 模拟页与合成九场景清单；正式九场景基础结构、动作、三维节点和资源映射未提供，不能把此结果计为任务-053的逐场景验收。真实设备标识只由平台内部维护，不进入我方清单。
2. 当前视图与流程协议兼容的正式网页图形构建已经完成；仍未对八个未交付场景执行真实业务内容联调，也没有正式部署环境来源。
3. 本轮所有断言通过，未产生失败画面；测试页已将失败收敛为有限摘要，但自动保存失败截图与持续集成归档仍待补齐。
4. 内部浏览器的单边渲染上限使 8K 请求在测试表中以可用高度约 4096 像素测量；生产样式本身没有像素上限，真实 8K 终端仍按同一比例公式计算。Unity 的实际渲染分辨率、显存和帧率须在任务-054按目标硬件复核。
5. 2026-08-08 的浏览器补测使用写死的合成设备，只验证旧状态更新时间协议与三层确认链路；不构成平台动态注入、真实燃气四态材质、连续完整快照或三维失败隔离验收。
6. 需在合作方真实平台完成结构清单回归：固定地址成功、两类404、单次原子响应、节点唯一性、静态三维映射和结构版本一致性。本地夹具已覆盖读取器与合成响应，不得替代该验收。
7. 需在合作方真实平台完成容量与快照回归：500项消息容量成功、超过容量整体失败且不截断；完整快照跨场景保存，切换后无平台重推仍立即恢复；同批重复节点最后一项生效；较小或相同 `sourceRevision` 仍直接应用；缺失节点恢复基线且不自动离线。本地完整快照链路仅覆盖两枚合成节点。
8. 需在合作方真实平台完成发送与回执端到端回归：平台清单注入后验证同一会话单槽位、五秒检查、十秒超时、新 `messageId`、显式不可恢复失败抑制和释放停止重试；源码专项已证明二维提交后立即产生唯一外层成功、三维未就绪/失败/超时不改变结果，仍须证明状态实际到达真实模型。
9. 需在受控内网或反向代理环境验证清单接口无业务鉴权且请求不携带凭据；合作方状态自测接口不得成为生产壳轮询路径。
10. 当前Unity基线已声明失败恢复激活标识、节点状态结构版本3、动态状态清除结构版本1和壳会话内本地快照序号；对象反向选择、真实平台节点状态及其迟到隔离仍须在合作方独立服务上验收。

## 结论

任务-052已用结构版本4正式Unity构建完成真实燃气初始化、23/22总图、三个关键环节过滤并复用总图坐标、紧凑直线路由、单Unity实例、三维聚焦描边、空白点击清除交互描边及二维/三维总览原子复位，进度保持65%。合作方真实结构清单读取、平台内部节点绑定、跨场景完整快照、单在途重试、真实三维状态成功及失败仍只返回一次二维成功尚未形成端到端证据；这些回归完成前不得关闭。
