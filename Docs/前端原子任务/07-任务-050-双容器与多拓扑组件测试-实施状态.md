# 任务-050：双容器与多拓扑组件测试实施状态

**状态：** 进行中（80%）
**更新时间：** 2026-08-05

## 已有组件与运行时覆盖

1. `topology-canvas-update-coordinator.spec.ts`（拓扑画布更新协调器测试）验证尺寸变化只调用尺寸更新，不会重建拓扑、选择或设备状态通道。
2. `topology-runtime.spec.ts`（拓扑运行时测试）验证预备阶段不操作画布、跨场景与旧事务不能覆盖活动画布、连续六次多图切换复用唯一画布端口、设备状态增量不改写拓扑、选择或视图状态，且重复释放只释放一次。
3. `topology-node-interaction.spec.ts`（拓扑节点交互测试）与外层事件发送器测试覆盖双击只产生显式设备意图和单次外层设备事件；任务-027的真实画布双击已验证合成设备只上报一次。
4. `TopologyPanel.vue`（拓扑面板）与 `TopologyCanvas.vue`（拓扑画布）保持一个画布实例；全屏、缩放、平移、Esc 退出和生命周期释放均由同一实例管理。

## 本轮内部浏览器视觉回归

测试对象为“外层本地宿主页 → 当前嵌入壳 → Unity 模拟页”中的正式双容器组件。外层测试控制栏带有自身滚动条，不计为嵌入壳滚动条；以下数值均从嵌入壳内部读取。

| 场景 | 实测结果 | 结论 |
| --- | --- | --- |
| 常规 1280×720 | 嵌入壳 1280×720；三维容器与拓扑容器按上下网格分配；拓扑面板高 301、画布高 170；仅存在 1 个 Canvas（画布），嵌入壳横向和纵向均无溢出 | 通过 |
| 尺寸不足 600×600 | 嵌入壳、拓扑面板、画布分别为 600×600、600×275、568×102；缩放工具栏完整位于画布 568×102 边界内；仅存在 1 个画布且无横纵溢出 | 通过 |
| 全屏重复进出 | 连续 6 次进入和退出拓扑全屏；进入时面板严格受当前视口四边各 12 像素约束，退出后恢复常规态；每次均只有 1 个画布 | 通过 |
| 缩放与键盘退出 | 两次放大后缩放为 130%，重置后为 100%；进入全屏后按 Esc 键退出，画布数量仍为 1 | 通过 |
| 浏览器错误 | 本轮控制台错误为 0 | 通过 |

本轮已经取得内部浏览器截图并完成画面审阅：三维区域保持 16:9，拓扑区域位于其下并承担剩余空间，拓扑全屏按钮和缩放控件均在可操作区域内。

## 已版本化视觉基准

新增只读视觉宿主页 `power-data-web/tests/visual-regression/host/index.html`。它复用本地三层夹具的真实来源、实例、会话、清单和 `system.init`（系统初始化）边界，但移除外层控制栏、时间戳与日志，避免动态调试内容污染截图。

| 基准图 | 视口 | 人工审阅结果 |
| --- | --- | --- |
| `power-data-web/tests/visual-regression/baselines/double-container-1280x720.jpg` | 1280×720 | 三维保留区为 742.39×417.59，宽高比约 1.7778；拓扑剩余高度 301.41；无外层滚动条 |
| `power-data-web/tests/visual-regression/baselines/double-container-3440x1440.jpg` | 3440×1440 | 三维保留区受动态视口高度限制并居中，仍为 16:9；拓扑区域持续保留可操作余量 |

复现步骤、基准适用边界和更新规则见 `power-data-web/tests/visual-regression/README.md`。内部浏览器当前导出 JPEG（联合图像专家组格式），基准均使用 `.jpg` 扩展名；不得把这一格式实现细节误判为页面图像编码能力。

## 自动视觉差异门禁

新增 `power-data-web/scripts/compare-visual-baseline.mjs` 与 `npm run test:visual`。命令只接受工作区内的两张 JPEG 截图，限制单文件不超过 25 MB、解码后不超过 1,200 万像素；它按像素尺寸兼容性、变更像素比例与平均绝对误差输出有限 JSON（JavaScript对象表示法）报告，并以非零退出码阻断阈值超限或尺寸错配。命令不写入基准、差异图或缓存，执行后只保留调用方显式提供的图片。

默认阈值为：单像素任意色彩通道差异超过 8 才计作变更像素、变更像素比例不高于 0.5%、平均绝对误差不高于 1.5。阈值必须由命令行显式传入才可调整，防止测试静默放宽。新增命令级测试覆盖同图通过、带鱼屏与常规图尺寸错配失败、绝对路径在读取前拒绝。

命令新增可选 `--diff`（差异图路径）参数，可在调用方预先创建的工作区目录中输出黑底亮差异 JPEG 图。输出采用“仅新建”策略，且拒绝基准图、当前图、已有文件、越界路径、缺失父目录和尺寸不一致的输入，避免差异归档覆盖用户基准或无界写入。命令级测试同时覆盖生成 JPEG 签名和覆盖基准图拒绝；本轮同图生成差异图后已删除临时输出。

## 完整回归

- 前端单元测试：35 个测试文件、156 项通过。
- Vue 类型检查通过。
- `npm run build:release -- --manifest tests/fixtures/scene-topology-contract-valid.json`（携带完整合成九场景清单的发布构建）通过；未传 `--manifest`（清单路径）时按发布门禁正常拒绝，不属于构建回归失败。
- 内部浏览器视觉宿主页 1280×720 回归：嵌入壳精确为 1280×720，三维保留区为 742.39×417.59、宽高比约 1.7778，拓扑区高 301.41，控制台错误为 0。
- 使用重新生成的 1280×720 当前截图执行 `npm run test:visual`：变更像素 0%、平均绝对误差 0，默认阈值内通过；当前截图已在验证后删除，避免把临时输出纳入版本库。
- 使用同一基准图执行 `npm run test:visual ... --diff artifacts/double-container-1280x720.local.diff.jpg`：生成有效 JPEG 差异图，变更像素仍为 0%；验证后已删除该临时差异图。

## 尚未关闭项

1. 已纳入版本库的基准图目前仍使用本地合成清单和 Unity 模拟页；自动差异阈值与可选差异图输出已交付，但正式多拓扑、九场景业务节点和真实网页图形构建尚未交付，不能把合成画面计为正式视觉验收。
2. 长时间切换后的内存、显存、绘制耗时和资源回调统计属于任务-054，不能以本轮短时组件回归替代。

## 结论

组件的关键交互、尺寸边界、唯一画布和释放路径已有单元与真实浏览器的双重证据；本轮补齐可版本化的常规与带鱼屏视觉基准、受限自动差异阈值、可选差异图输出及命令级回归，任务进度保持 80%。待补齐正式多图资料与长时间性能验收后继续推进。
