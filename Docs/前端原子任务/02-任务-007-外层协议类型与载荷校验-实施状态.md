# 任务-007：外层协议类型与载荷校验实施状态

**状态：** 已完成（100%）  
**更新时间：** 2026-08-15  

## 完成结论

- 外层协议继续使用固定通道、版本、实例、会话、消息标识、响应关联和逐消息类型载荷校验。
- `device.states.update` 已按合作方最终约定收紧为当前资源完整快照：`sourceRevision` 必填且只能是非负安全整数，但协议入口不比较其大小，也不据此拒绝后到快照。
- `nodeId` 使用资源内稳定标识规则，状态、双击和三维反向选择均以 `nodeId` 为关联键；真实 `deviceId` 不进入我方协议。
- `statusUpdatedAt` 必须是显式携带 `Z` 或 `±HH:mm` 时区的国际标准时间，并校验闰年、月份、日期、小时、分钟、秒和时区范围，避免网页运行时自动修正非法日期。
- 状态数组1至500项通过，501项及以上整体拒绝；协议入口不会产生部分载荷，也不会把非法状态推断为离线。
- 上行双击和三维反向选择事件同样复用节点标识校验，防止未知节点从出站路径绕过。

## 代码与测试

- `power-data-web/src/host-bridge/host-protocol.ts`：完整快照载荷、节点编号、来源修订号、时区时间和容量门禁。
- `power-data-web/src/config/scene-topology/identifiers.ts`：节点标识工厂与非抛出式守卫。
- `power-data-web/src/config/scene-topology/validator.ts`：清单拒绝 `deviceId`、`deviceMappings`、`platformBindingCount` 和 `emit-device`，并校验节点全局唯一及静态三维反查。
- 相关测试夹具使用资源内合法 `nodeId`；旧设备字段仅保留为拒绝用例，非法日期和容量边界继续覆盖。

实际验证结果：

- 前端单元测试：39个测试文件、208项全部通过。
- 类型检查：通过。
- 生产构建：通过，共转换95个模块。
- 差异检查：无空白错误；仅保留仓库既有的换行格式提示。

## 保留边界

本任务只关闭协议类型与载荷校验，不提前实施以下任务：

- 重复 `messageId` 静默丢弃和状态命令单在途：任务-011。
- 完整快照原子替换、缺失节点恢复基线和跨场景重投影：任务-029。
- 二维成功立即回执、三维异步尽力同步和本地快照序号：任务-038。
- 单结构清单、节点唯一性和静态三维反查：任务-049。
- 三层固定接口与浏览器联调回归：任务-014、052。

本次未修改用户现有的 Unity 网页桥初始化改动，也未重新构建 Unity 正式网页图形包。
