# Unity 三层数字孪生场景重构计划大纲

**文档状态：** 方案已冻结，第三层已支持从任意稳定场景直达已配置关键环节，正式六套资源与剩余通用化验收待完成
**制定日期：** 2026-08-26
**最近调整：** 2026-09-01
**适用项目：** `WebDLPro` Unity WebGL 三维运行时与 `power-data-web` 嵌入壳
**关联任务：** 重构任务调整为 `R-001`—`R-021`；保留 `R-001`—`R-014` 已形成的第一层、第二层基础，新增 `R-015`—`R-018` 第三层关键环节任务，原多页面与发布任务顺延为 `R-019`—`R-021`。

## 一、结论与目标

本次重构采用“轻量沙盘总览 → 业务厂区与整套内部设备 → 关键环节独立细节模型”的三层表现结构：

视图展示规则固定为：第一层沙盘和第三层关键环节均由三维画面占满子应用可视区，不展示拓扑图；只有第二层厂区同时展示三维画面和当前业务拓扑。本文所称“全屏三维”是占满子应用可视区，不代表自动调用浏览器原生全屏接口。

1. 第一层新增独立沙盘场景，只放九个代表性建筑的低模、必要管道和区域效果，不放九个完整业务场景的设备细节；页面停用拓扑画布并让三维画面占满子应用可视区。
2. 九个代表性建筑通过配置绑定其所代表场景的设备组。只有收到异常状态时，才根据配置触发建筑异常视觉、受影响管道停流和受影响区域效果。
3. 第二层进入对应业务场景后，页面同时显示厂区三维画面和当前业务拓扑；三维中先显示厂区地面、厂房整体壳体和必要主干管道，厂房壳体资源常驻，不因进入内部而卸载。
4. 点击厂房后按需加载当前业务场景的整套内部设备，加载成功并完成最新状态重放后隐藏或半透明显示厂房壳体。整套内部设备仍属于第二层，不等同于第三层关键环节模型。
5. 第三层采用“每个业务场景可配置 0～多个关键环节”的通用结构。当前首批只交付六套相互独立的关键环节细节模型：燃煤三套、燃气三套；后续其他场景可按实际业务配置一个、两个或更多，不设置固定数量校验。进入任一关键环节后停用并隐藏拓扑画布，三维画面占满子应用可视区；它们不是从第二层整套内部设备中凸显单个模型，也不再以隐藏其他模型作为主要表现方式。
6. 平台继续以 `workflow.trigger` 和 `actionId` 作为唯一入口；前端仅从已发布动作映射取得内部 `processDetailId`，再调用新增的白名单关键环节命令。该命令不得复用旧 `enterProcessStep` 的模型显隐、包围盒聚焦或流程重置语义；Unity 通过显式映射按需加载当前一套资源，完成状态重放后才切换到专用观察位。
7. 当前首批六套关键环节模型放在远离各自厂区的独立展示区；后续新增环节沿用同一空间隔离规则，避免相机观察时与第二层厂区产生空间重叠。远端坐标和相机观察位必须显式配置，不能根据模型包围盒或场景名称临时推断。
8. 关键环节模型中由 3ds Max 制作的旋转、位移动画，以及管道流动和粒子特效，统一登记到该环节的运行时控制器；同一实例默认最多激活一套关键环节资源，切换或退出时释放上一套。
9. 关键环节设备状态必须与页面收到的最新设备状态一致，但设备四态只控制状态视觉，不控制模型动画、管道流动或粒子特效的播放与停止。动态效果由独立播放控制器管理；当前本地自测页已通过受控外层命令提供播放/停止按钮，正式模型点击交互仍按显式映射单独推进。
10. 厂房允许是一个 Mesh、多个材质球，但只做整体显示、整体隐藏和整体半透明，不做屋顶、墙体等局部控制。
11. 设备告警、故障的红色描边、内部颜色和闪烁继续使用现有高亮插件能力；透明度、颜色、闪烁参数和流速差异由少量共享材质变体与材质属性块控制。
12. 每个页面独立接收外部平台设备状态。五个页面可以同时运行五个相互独立的 Unity WebGL 实例，不要求平台方提供共享连接能力。

## 二、现有能力与本次重构的衔接

当前工程已有以下可复用基础：

- `Bootstrap` 常驻启动场景和九个业务场景目录。
- `MultiSceneCoordinator` 单实例业务场景切换、事务取代、失败恢复和释放。
- `SceneBundleRuntimeLoader` 场景资产包加载、依赖校验、缓存和租约释放。
- `PowerPlantProcessController` 设备四态视觉、高亮插件接入、材质恢复、管道流速属性控制和历史流程过滤入口。R-016 前仍可能包含旧模型显隐、其余模型半透明、包围盒聚焦和全场景故障停流；R-016 起这些逻辑禁止被第三层调用，R-021 回归通过后按引用审计结果删除其第三层专用部分。
- `PowerPlantFreeCameraController` 单相机复位、包围盒取景和补间移动能力；第三层必须新增显式观察位切换与返回基线，不能沿用“聚焦当前模型”的旧语义。
- `BusinessSceneDetailRuntime`、`BusinessSceneInteriorRuntime` 第二层整套内部设备的按需加载、状态重放和资源释放基础；其单资源目录不得复用于第三层。
- `TopologyDeviceStateCache` 完整状态快照、场景节点投影、缺失节点基线恢复和有限容量。
- `DeviceStatesUpdateCoordinator` 三维状态异步尽力投递、单槽合并和有限工作池。
- `runtime-connector` 内层场景切换、流程动作、状态下发和稳定标识协议。

本次重构不改变以下既有原则：

- `nodeId` 是外部平台和前端状态链路的唯一逻辑设备标识。
- `sceneNodeId` 只表示我方场景内稳定三维节点。
- 不从模型名称、文件名、层级路径、截图或坐标关系猜测业务映射。
- 合法完整快照原子替换，缺失节点恢复基础视觉，三维失败不阻塞二维外层成功。
- 场景、监听器、材质、协程、队列、资源句柄均必须有明确释放路径和容量上限。

参考文档：

- [Unity 多场景运行时](03-Unity多场景运行时.md)
- [单发布版本与场景资源拆分实施状态](03-任务-020-单发布版本与场景资源拆分-实施状态.md)
- [设备状态批量协调与可观测性](03-任务-038-设备状态批量协调与可观测性-实施状态.md)
- [节点标识关联协议偏离调整](08-任务-056-节点标识关联协议偏离调整.md)
- [Unity 设备状态视觉搭建标准](Unity设备状态视觉搭建标准.md)
- [场景模型节点与拓扑节点命名规范](场景模型节点与拓扑节点命名规范.md)

## 三、目标运行时结构

```text
Bootstrap 启动场景
├─ 外部桥接与状态接收
├─ 场景协调、流程动作与加载反馈
└─ 公共轻量资源

第一层：Overview 沙盘场景
├─ 九个代表性建筑低模
├─ 代表建筑点击代理
├─ 异常影响管道
├─ 受影响区域覆盖网格和边界网格
└─ 沙盘视觉控制器

第二层：业务场景（九个之一）
├─ 厂区地面
├─ 厂房整体壳体
├─ 主干管道
├─ 厂房点击代理
├─ 整套内部设备根节点（初始未加载）
├─ 第二层设备状态绑定
└─ 业务场景视觉控制器

第三层：关键环节独立细节资源（每个业务场景可配置 0～多个）
├─ 当前首批：燃煤关键环节细节模型 × 3
├─ 当前首批：燃气关键环节细节模型 × 3
├─ 后续场景：按实际业务配置 0、1、2 或更多套
├─ 3ds Max 导入的旋转与位移动画
├─ 环节内部管道流动与粒子特效
├─ 设备状态绑定与四态视觉控制
├─ 独立播放控制与可选模型点击代理
├─ 远端展示锚点与专用相机观察位
└─ 独立加载句柄、状态重放与释放边界
```

`Overview` 是新增的全局总览场景，不计入九个业务场景的业务编号。现有场景目录校验固定九个业务 `sceneId`，实施时应为全局场景增加独立类型或独立目录入口，不能把沙盘伪装成某个业务场景，也不能把沙盘重资源放进 `Bootstrap`。

第二层整套内部设备继续使用每个业务场景唯一的 `detailResourceId`。第三层必须新建独立的场景级可变长度目录，由 `(sceneId, processId, stepId)` 显式映射到内部 `processDetailId`、资源标识、观察位、设备绑定和动态目标清单；一个场景可登记 0～多个关键环节，目录校验不得写死三个。运行时协调器必须消费目录数据，不得写死燃气场景、燃气轮机或单一状态节点。现有 `ThreeLayerBindingCatalog` 只保留第一、二层的节点、设备组、管道、区域和影响规则；第三层目录不能覆盖第二层目录，也不能把关键环节模型注册成新的业务 `sceneId`。

`Bootstrap` 只负责初始化桥接、场景协调器和公共轻量服务，启动后保持空闲，不默认进入 `Overview` 或任一业务场景。首次进入、第二层业务场景切换和返回 `Overview` 均由平台复用外层 `system.init` 或 `view.open` 命令明确指定；平台命令的 `sceneId="overview"` 必须省略 `topologyId`，以 `view.open` 进入第二层业务场景时必须携带合法 `topologyId`。第三层关键环节由 `workflow.trigger` 进入，其稳定状态不携带 `topologyId`。子应用在同一原子事务中生成内部 `transitionId`、场景映射版本并向 Unity 下发内层 `switchScene`，平台不得直接调用 Unity。

关键环节切换继续复用 `workflow.trigger` 的外层原子事务，但动作映射必须显式产出内部 `processDetailId`，并由新增白名单关键环节命令执行。若当前稳定上下文不属于目标业务场景，前端必须先通过受控场景事务进入目标场景默认拓扑并确认新 `sceneActivationId`，阻塞式重放该场景最新权威设备状态后，才允许启动关键环节事务；不得要求用户手动预先进入目标场景。完整顺序固定为“校验动作映射 → 确保目标业务场景稳定 → 重放目标场景最新设备状态 → 加载目标关键环节资源且保持隐藏并重放环节状态 → 准备显式相机位 → 原子停用拓扑并切换为全屏三维布局 → 提交细节资源与相机”。关键环节稳定状态不提交 `topologyId`，也不激活总拓扑或流程过滤拓扑。旧 `enterProcessStep` 只允许作为历史兼容代码保留，不能作为第三层提交路径；R-016 起第三层不得调用旧半透明、显隐、包围盒聚焦或拓扑过滤逻辑，R-021 在完成回归和引用审计后删除不再使用的旧第三层专用代码；任何场景切换、状态重放、加载、相机准备或布局切换失败都不得提交半完成的第三层视图。

沙盘建筑点击只上报稳定建筑选择意图，不在 Unity 内部直接切换目标业务场景。平台收到意图后，应结合当前稳定上下文决定是否发出下一条 `view.open`；这样场景、拓扑、遮罩和失败恢复始终属于同一原子事务。

### 3.1 业务场景返回全局沙盘的方案一

用户已确认：全局沙盘只有三维场景，没有对应的全局拓扑图。平台触发业务场景返回时复用 `view.open({ sceneId: "overview" })`；网页在 Unity 成功前保留并遮罩旧稳定视图，收到匹配的新 `sceneActivationId` 后停用并隐藏拓扑区域、清空业务拓扑选择，不伪造或临时生成全局拓扑。Unity 不提供自动返回或本地旁路。

返回事务仍必须经过统一联动协调器，不能从平台绕过外层事务调用低层 `switchScene("overview")`。固定顺序为“校验外层命令 → 开始新事务 → 切换 `Overview` → 停用单画布拓扑并清空选择 → 提交无 `topologyId` 的全局沙盘稳定状态”。

切换期间不得暴露“全局沙盘 + 旧业务拓扑”的可操作组合。只有收到匹配的新 `sceneActivationId` 并确认 `Overview` 就绪后，才允许移除切换遮罩并完成返回；失败、超时、快速重复返回、旧回调和释放竞态沿用现有事务取代与恢复规则。

返回成功后，网页不提交新的 `topologyId`，不保留业务拓扑的可操作状态；业务场景选择、流程动作和业务拓扑交互全部清空或隐藏。全局沙盘建筑状态只使用已确认的三层绑定投影，不依赖拓扑清单。

### 3.2 三种视图模式

| 视图模式 | 对应层级 | 三维画面 | 拓扑画布 | 稳定状态要求 |
| --- | --- | --- | --- | --- |
| 沙盘全屏三维 | 第一层 `Overview` | 占满子应用可视区 | 停用并隐藏 | 不提交 `topologyId`，清空业务拓扑选择 |
| 厂区三维与拓扑并存 | 第二层业务场景 | 使用厂区三维区域 | 显示当前业务拓扑 | 必须提交合法 `topologyId`，三维与拓扑同一事务稳定 |
| 关键环节全屏三维 | 第三层关键环节 | 占满子应用可视区 | 停用并隐藏 | 提交 `processDetailId`，不提交 `topologyId` |

三种模式共用一个 Unity 实例和一个可复用拓扑画布。隐藏拓扑时必须保留同一个画布组件实例，只暂停输入、尺寸监听、布局和重绘；禁止通过条件卸载后重新挂载画布。返回第二层时恢复进入关键环节前的厂区拓扑、选择和三维观察位。模式切换必须纳入同一 `transitionId`，不得短暂暴露“沙盘或关键环节三维画面 + 厂区旧拓扑”的可操作组合。

## 四、状态投影与异常影响

### 4.1 分层状态投影

每个页面独立接收平台状态并维护本页状态缓存。状态进入 Unity 前，先按当前页面和当前层级生成最小投影：

- 第一层沙盘：设备组聚合为代表建筑状态，并产生异常影响结果。
- 第二层厂区壳体：当前业务场景设备组聚合为厂房状态。
- 第二层整套内部设备：设备节点直接使用自身四态。
- 第三层关键环节：只投影当前已加载细节模型显式登记的设备节点；加载完成、切换环节或状态更新后均以最新完整快照为准。

第三层模型中的设备绑定仍使用已有 `sceneNodeId` 语义；同一设备在第二层与第三层可以拥有不同 Renderer 和动画对象，但必须由显式绑定表关联到同一逻辑状态来源，禁止根据模型名称自动匹配。

### 4.2 建筑聚合优先级

```text
故障 > 告警 > 明确离线 > 正常
```

缺失状态不能自动解释为离线。只有平台明确上报离线时，才允许进入离线分支。

### 4.3 异常影响配置

第一版只为异常状态配置影响规则，其他状态保持空配置但保留扩展能力：

```text
visualStateRules
├─ normal: 无异常影响
├─ alarm: 可选影响规则
├─ fault: 明确影响规则
└─ offline: 预留，不默认启用
```

异常规则至少包含：

- 触发设备组或场景节点集合。
- 代表性建筑。
- 受影响管道或路由。
- 受影响区域。
- 是否停止流动。
- 区域效果类型和材质参数。
- 恢复条件。

所有配置均使用稳定 ID，不保存任意层级路径作为运行时协议字段。

## 五、视觉实现方案

### 5.1 模型状态

设备、代表性建筑和厂房的异常视觉继续由现有高亮插件实现：

- 描边颜色。
- 内部颜色覆盖。
- 告警和故障闪烁。
- 高亮启停。

状态控制器只负责将平台状态映射为视觉状态，不让业务协议直接操作插件对象。

### 5.2 共享材质变体与材质属性块

材质变体只区分渲染模式，例如不透明、上下文半透明、异常半透明和管道流动。材质属性块控制实例差异：

- 颜色。
- 透明度。
- 闪烁频率和强度。
- 发光强度。
- 流速和流向。

材质变体必须覆盖渲染队列、混合模式和深度写入等渲染状态；材质属性块不承担这些状态切换。运行时禁止无界创建材质副本，原始材质数组和属性块必须可恢复。

**已导入材质适配记录（2026-08-26）：**

- `Assets/Shaders/Imported/GuanDaoFlow/LYDS_GuanDao02.mat` 使用 `LYDS/GuanDao`。`_Speed` 和 `_Speed2` 保持二维向量流向；新增标量 `_FlowSpeed`，默认值为 `1`，统一乘到两层纹理的 UV（纹理坐标）平移计算中。适配器只将 `_FlowSpeed` 映射为 `FlowSpeed`，禁止把向量属性误当成标量流速。
- `Assets/Shaders/Imported/RailArea/Rail.mat` 使用 `Custom/Rail`。`_mainColor` 映射为颜色，`_direction` 映射为方向；新增标量 `_FlowSpeed`，默认值为 `0`，因此默认表现为静态区域边界，需要动画时再由显式配置通过材质属性块写入非零值。
- `Rail` 的 `_AlphaTexture` 继续控制轮廓透明度，`_MainTexture` 只参与亮度变化；本次适配不改变其透明混合、深度写入和渲染队列。
- 两个材质均通过 `ThreeLayerMaterialPropertyAdapter` 使用 `MaterialPropertyBlock`（材质属性块）写入，不调用 `renderer.material`，不修改共享材质，不创建运行时材质副本。
- 当前只完成 Shader 与适配器契约验证；由于正式管道 Renderer、`pipeId`、`routeId`、区域 Renderer 和 `areaId` 映射仍未提供，暂不把这两个材质直接绑定到业务场景或伪造影响关系。

### 5.3 区域效果

第一版只实现两种正式效果：

1. **半透明区域覆盖：** 美术提供区域网格，使用共享半透明材质和羽化边缘。
2. **静态区域边界：** 美术提供边界线或轮廓网格，使用共享无光或轻发光材质。

当前已导入静态区域边界候选材质 `Assets/Shaders/Imported/RailArea/Rail.mat`。其 `_FlowSpeed=0` 为默认静态约束；正式区域效果运行时仍需等待区域网格、`areaId`、效果配置和 Renderer 显式绑定资料，不因材质已导入而自动扫描或绑定场景对象。

第三种效果只预留配置接口，可选为静态阻断标记，不默认制作粒子、脉冲、持续扫描线或高频动画。

区域效果只在异常影响结果非空时启用。没有异常时不启用区域 Renderer，不执行区域全量每帧遍历。运行时通过 `ThreeLayerAreaEffectRuntime` 按显式 `areaId` 登记一个或多个材质槽，默认写入隐藏状态；异常集合变化时只更新状态发生变化的区域，解除影响后恢复登记时的颜色与透明度基线。正式区域网格和映射资料未到齐前，不绑定测试对象或扫描沙盘层级。

### 5.4 第三层独立播放控制与动态资源生命周期

每套关键环节模型中的动态对象必须在资源导入或装配阶段显式登记，不能在状态热路径中遍历层级查找：

- 3ds Max 导入的旋转、位移动画片段及其默认播放状态。
- 环节内部流动管道、默认流速和流向。
- 粒子系统、默认播放状态和停止策略。
- 可选播放点击代理、稳定交互目标 ID 和明确碰撞体。
- 设备 Renderer、高亮目标及其 `sceneNodeId`。

设备四态和播放状态是两条独立状态链路。正常、告警、故障、离线及状态清除均不得隐式改变动态效果的播放许可；播放或停止只接受当前关键环节内部受控交互或未来单独评审的外层命令。后续若采用模型点击，固定链路为“显式碰撞体命中 → 稳定交互目标校验 → 当前活动环节校验 → 本地播放控制器”，不得根据模型名称或层级路径推断控制对象。

暂停动画优先使用统一运行时控制器保存并恢复速度或播放状态；粒子停止时不得无界创建新实例；管道仍通过材质属性块将流速置零。重复播放或停止必须幂等。切换、返回、失败和资源释放必须停止并清理当前实例的动态状态、点击监听和材质属性块，避免影响下一次实例。

## 六、管道流动方案

每根管道由美术拆分 UV。业务绑定和流动材质实现分离：

```text
pipeId / routeId
    ↓
影响关系和停流控制
    ↓
流动材质适配器
    ↓
具体流动材质球
```

管道配置保存：

- 稳定管道 ID。
- 路由 ID。
- 所属设备组或影响区域。
- UV 流动方向。
- 默认流速。
- 材质配置和属性名映射。

后续更换流动材质球时，只调整材质配置和属性映射，不修改设备绑定、异常影响关系或平台协议。

当前已验证的正式候选材质为 `Assets/Shaders/Imported/GuanDaoFlow/LYDS_GuanDao02.mat`。它通过统一 `_FlowSpeed` 标量接入停流和倍率控制，原有 `_Speed`、`_Speed2` 向量只负责流向；替换材质时必须继续提供等价的标量流速属性，或在材质适配配置中登记明确的标量属性名。

第一层和第二层停流采用路由级影响集合计算，不直接关闭管道对象；多个异常共同影响同一路由时，只有全部影响解除后才恢复原始流速。第三层关键环节管道属于独立播放控制，不接受设备四态隐式停流。各层分别维护自身登记表，禁止跨层通过 Renderer 名称共享运行时状态。

## 七、第二层厂房壳体与整套内部设备方案

厂房壳体可以是一个 Mesh、多个材质球。运行时以厂房整体为一个绑定目标：

- 遍历并缓存全部材质槽的原始材质。
- 每个材质槽使用对应的透明材质变体。
- 通过材质属性块统一修改整体透明度和状态颜色。
- 支持整体不透明、整体半透明、整体隐藏和整体恢复。

本次不要求屋顶、墙体、设备遮挡面等局部透明。如果未来出现局部显示需求，需要另行提供拆分 Renderer、材质槽、顶点颜色或遮罩资源。

厂房壳体资源在点击加载整套内部设备后继续保留，不卸载。壳体可切换为隐藏或半透明；整套内部设备通过第二层独立加载句柄按需加载，在返回厂房或退出业务场景时释放。该资源负责完整厂区内部浏览，不承担第三层关键环节的高精细展示。

当前燃气场景过渡状态：`GasPower.unity` 已按第二层要求关闭启动阶段的历史总览半透明，进入场景时保留现有资产原始材质。但由于正式第二层设备资源、目录、加载器、状态重放器和独立挂载点尚未交付，内部设备仍随场景预加载；本状态不代表 R-011 至 R-014 的正式按需加载验收完成。

## 八、第三层关键环节细节模型方案

1. 第三层目录按业务场景保存可变数量的关键环节，允许为零。当前交付范围是燃煤三套、燃气三套，共六套；后续其他场景可根据实际需求只登记一套、两套或更多。外层继续以 `workflow.trigger` 和 `actionId` 作为平台入口，动作定义再显式映射到 `processDetailId`、资源标识和新白名单关键环节命令，不复用旧 `enterProcessStep` 作为第三层执行语义。
2. 每套资源包含独立根节点、远端展示锚点、专用相机观察位、设备状态绑定、动画、管道、粒子及释放信息。
3. 资源只在收到对应关键环节命令后加载。默认同一 Unity 实例最多保留一套活动细节资源；切换环节时先准备新资源，提交成功后释放旧资源，避免空窗和并发实例。
4. 每套模型放置在远离所属厂区且互不干扰的预设区域。相机切换使用显式位置和旋转，不再依赖旧逻辑的“聚焦一个模型并淡化或隐藏其他模型”。
5. 第二层整套内部设备可继续保持已加载状态，但第三层观察时不对其执行大规模逐对象显隐；通过空间隔离和相机位置保证不会看起来处于同一展示区域。
6. 第三层只显示全屏三维画面，不激活或展示任何拓扑；拓扑画布停用期间不接收指针输入、不执行布局和重绘。
7. 返回第二层时，相机恢复进入第三层前的业务场景观察位，释放当前关键环节资源，恢复进入前的厂区拓扑及选择，并重新投影第二层最新状态。
8. 关键环节加载失败、状态重放失败、相机准备失败或切换事务被取代时，继续保留上一稳定视图并清理迟到资源。
9. 已处于第三层时允许直接切换到同场景另一关键环节：先隐藏准备新资源并完成状态重放与相机准备，提交成功后再释放旧资源，不强制绕回第二层。
10. 进入事务拆分为“准备”和“提交”两阶段。准备阶段不得显示目标资源或移动相机；拓扑暂停和全屏布局就绪后，提交阶段才原子显示资源并切换相机。
11. 第三层不得依赖停用整个第二层业务根节点实现空间隔离。第二层资源可保持加载，通过显式远端展示锚点和相机位避免空间重叠。

### 8.1 旧关键环节半透明逻辑迁移与删除门禁

旧关键环节表现中的“保留目标模型、其余模型半透明”属于第二层历史流程过滤实现，不属于第三层独立细节模型方案。

- **R-016 行为停用：** 新第三层命令接通后，第三层路径不得调用旧流程的其余模型半透明、全量显隐、`FocusBounds`（包围盒聚焦）或全场景管道停控。`enterProcessStep` 仅在仍有已发布二维过滤兼容引用时保留，且不得被第三层动作映射引用。
- **R-017—R-018 回归隔离：** 第三层状态、故障视觉、管道、动画和粒子必须只访问第三层自己的登记表与动态目标控制器，不能通过旧流程入口间接触发半透明或全场景停控。
- **R-021 代码删除：** 完成新第三层、第二层和历史二维兼容回归后，执行静态引用审计。没有有效引用的旧第三层专用方法、字段和调用链必须删除；仍被历史二维兼容使用的部分只能保留最小实现，并在任务清单中记录唯一用途。删除前必须有测试证据，禁止凭名称猜测后直接删除。

### 8.2 单链路纠偏进展

历史上的“全局沙盘 → 燃气厂区与拓扑 → 燃气轮机关键环节”仅作为首条纵向验证链路，不代表 R-015—R-018 已完成。2026-09-01 已补齐跨场景直达：从沙盘或任意其他稳定业务场景触发已登记关键环节动作时，前端会自动切换并稳定目标业务场景、等待最新状态重放，再进入第三层，无需用户手动预先进入对应场景。该能力已通过前端定向与全量自动测试，正式浏览器跨场景实机往返及首批六套资源联调仍待完成。后续继续按原任务边界推进，不新增重复任务：

- **R-015：** 将燃气轮机单项目录和硬编码协调器改为场景无关、可变数量目录驱动；补齐首批六套资源与多设备、多动态目标结构。
- **R-016：** 保留唯一拓扑画布实例；已补齐跨场景关键环节直达、同场景直接切换、远端锚点、第二层不整体停用、准备/提交两阶段事务、目标场景状态重放屏障和迟到补偿；继续完成正式六套资源及浏览器回归。
- **R-017：** 将单一状态节点扩展为每套资源显式设备绑定列表，并接入现有高亮能力；简单材质染色只能作为资源缺失时的临时降级，不作为正式验收结果。
- **R-018：** 保留已完成的四态与播放解耦，清理仍以正常/故障模拟播放/停止的测试入口；后续优先实现显式模型点击控制，但当前不要求新增外层播放协议。

## 九、五页面运行策略

五个页面指五个独立浏览器标签页或父页面实例；每个实例内部仍只有一个 `VisualizationRuntimeHost` 和一个 Unity WebGL 实例。它们各自接收外部平台状态、维护页面会话和 Unity 实例：

- 不要求平台方改造为共享 WebSocket（网页套接字）连接。
- 不要求新页面读取其他页面的缓存。
- 每个页面只向当前 Unity 实例投影当前场景需要的状态。
- 页面关闭时释放本页监听器、状态缓存、Unity 桥接和渲染资源。

五个实例的压力主要来自运行时堆、纹理、网格、显存和图形上下文，而不是模型下载。必须以目标浏览器和目标设备进行五实例实测，不能仅凭资源文件大小判断可行性。

## 十、性能与资源目标

### 10.1 强制原则

- 沙盘只加载低模建筑和必要效果模型。
- 九个业务场景不同时驻留。
- 厂房壳体可常驻，第二层整套内部设备按需加载。
- 第三层关键环节资源不同时常驻，默认只保留当前活动的一套；目录容量使用明确上限，但不按每场景固定数量实现。
- 第三层通过远端空间和相机观察位隔离，不为表现切换全量遍历并隐藏第二层模型。
- 沙盘和关键环节使用全屏三维布局；隐藏拓扑时暂停其输入、布局和重绘，返回厂区时复用原画布并自动适配居中，不销毁重建；拓扑区域同时提供手动重置位置入口。
- 3ds Max 导入动画、粒子和管道必须一次登记、按独立播放命令增量控制，不在每帧查找组件。
- 异常影响只更新变化的建筑、设备、第一层/第二层管道和区域，不改变第三层播放状态。
- 状态更新不触发全场景材质重建。
- 区域效果不使用粒子和高频脉冲作为默认实现；第三层模型自带粒子只在显式登记后运行。
- 材质和管道属性使用缓存的材质属性块。
- 场景、第二层整套设备或第三层关键环节加载失败时保留可用的上一稳定视图。
- 所有队列、缓存、绑定索引、加载句柄和诊断记录有容量上限。

### 10.2 必测指标

- 沙盘首帧时间和峰值内存。
- 第二层厂区壳体首帧时间和峰值内存。
- 第二层整套内部设备加载时间和峰值内存。
- 当前首批六套及后续全部已配置关键环节各自的加载、状态重放、相机切换和释放耗时。
- 第三层动画、粒子和管道同时运行时的帧率、绘制调用和垃圾回收分配。
- 独立播放、停止、重复点击、切换和释放时，第三层动画、粒子和管道控制的正确性与耗时。
- 厂房壳体半透明时的帧率和透明排序正确性。
- 连续进入、退出第二层内部设备和第三层关键环节后的内存是否持续增长。
- 五页面同时运行时的总进程内存、显存、帧率和图形上下文稳定性。
- 五页面同时收到异常状态时，状态投影和视觉恢复是否一致。

具体数值在目标浏览器、目标硬件和正式模型到齐后冻结，未测量前不得用经验值替代验收。

## 十一、实施阶段

| 阶段 | 任务 | 目标 |
| --- | --- | --- |
| 0. 方案与输入冻结 | `R-001`—`R-003` | 冻结三层边界与职责；随后冻结首批六套关键环节清单、映射资料、资源交付和材质/动画属性契约 |
| 1. 配置基础 | `R-004`—`R-006` | 建立三层 ID、设备组聚合、异常影响索引和共享视觉契约 |
| 2. 第一层沙盘 | `R-007`—`R-010` | 完成建筑异常、管道停流和两类区域效果 |
| 3. 第二层厂区 | `R-011`—`R-014` | 完成壳体常驻、整套内部设备按需加载、状态重放和厂房下钻 |
| 4. 第三层关键环节 | `R-015`—`R-018` | 在已打通燃气轮机单链路基础上，完成目录通用化、首批六套资源、画布复用、直接切换、远端相机、多设备状态、高亮视觉和独立播放控制，并在 R-016 起停用旧半透明表现路径 |
| 5. 多页面验证 | `R-019`—`R-021` | 完成五实例状态、性能、异常、发布和回归验收 |

## 十二、明确不在本次范围内的内容

- 不重做现有外层通信协议的安全边界。
- 不把平台内部真实设备编号写入我方结构清单或 Unity 协议。
- 不为第一层加载九个完整业务场景。
- 不取消第二层“点击厂房后加载整套内部设备”的能力。
- 不再把第三层关键环节实现为“凸显第二层单个模型并隐藏其他模型”。R-016 起新第三层不得调用该旧表现路径；R-021 在引用审计后删除不再被历史二维兼容使用的旧第三层专用代码。
- 不把关键环节模型注册成新业务场景，也不把它们合并进第二层整套内部设备资源。
- 不把“每个场景固定三个关键环节”写入目录、协议或运行时；数量由各场景正式配置决定。
- 不要求本次为燃煤、燃气以外的其他场景制作第三层正式模型，但基础架构必须支持这些场景后续配置一个、两个或更多关键环节。
- 不默认同时加载或播放多套关键环节模型。
- 不根据模型名称、层级路径、坐标或动画片段名称自动推断设备、管道、粒子和流程映射。
- 不为第一层区域效果默认实现粒子、脉冲、扫描线等高频特效；第三层模型自带粒子按正式资源清单控制。
- 不要求厂房局部 Mesh 拆分或局部透明。
- 不在本次重构中强制引入 Addressables（可寻址资源系统）；优先延续现有 AssetBundle（资产包）资源治理。
- 不要求五个页面共享外部平台连接。
- `power-data-web` 的 `/gas-topology-json-preview` 仅用于验证原始 JSON 拓扑的替代显示方案；它不接入正式场景清单、状态缓存、Unity 桥接或发布入口。若后续确认替换正式拓扑，必须新建独立任务并补齐资源预算、交互、状态投影、无障碍与发布回归。
- 不在未取得正式美术资源和业务映射前猜测管道、区域、设备、动画或粒子归属。
- 不在未确认全局沙盘返回命令契约前实现平台返回入口；全局沙盘没有对应拓扑，不新增伪造的全局 `topologyId`。
- 返回全局沙盘时只显示三维沙盘并隐藏网页拓扑，清空业务拓扑选择，禁止出现“全局沙盘 + 旧业务拓扑”的可操作组合。

## 十三、最终完成标准

只有同时满足以下条件，才认为本次重构完成：

1. 第一层沙盘可展示九个低模代表性建筑，并能按设备组聚合状态。
2. 异常建筑能按配置触发对应管道停流和区域效果，正常状态不产生未配置效果。
3. 管道 UV、路由绑定和流动材质可独立替换。
4. 第二层厂房壳体可整体显示、隐藏和半透明，多个材质槽恢复正确。
5. 点击厂房后第二层整套内部设备按需加载，加载完成后能重放最新状态；返回时资源释放正确。
6. 第三层目录支持每个业务场景配置 0～多个关键环节，不存在固定三个的代码限制；当前燃煤三套、燃气三套均能由 `workflow.trigger → actionId → processDetailId` 的显式映射唯一按需加载，不依赖模型名称或坐标猜测，也不走旧 `enterProcessStep`。
7. 第三层相机能切换到远端专用观察位；第二层厂区不会与关键环节模型看起来处于同一展示空间，返回时相机恢复正确。
8. 关键环节设备首次显示和后续更新均与最新设备状态一致，视觉继续使用现有四态与高亮规则。
9. 关键环节设备四态只改变状态视觉，不隐式改变管道、3ds Max 导入动画或粒子特效的播放状态；独立播放和停止幂等，切换、返回与释放无动态状态残留。
10. 同一实例默认最多激活一套关键环节资源，快速切换、失败、返回和迟到回调不会产生重复实例或状态串场；R-016 起第三层不调用旧半透明、全量显隐、包围盒聚焦或全场景管道停控，R-021 完成引用审计并删除不再需要的旧第三层专用代码。
11. 五个页面可独立接收状态并同时运行，互不污染场景、第二层设备和第三层关键环节状态。
12. 业务场景返回全局沙盘时，能通过同一原子事务切换 `Overview` 三维场景、隐藏网页拓扑并清空业务拓扑选择；切换期间不暴露旧业务拓扑，失败时能恢复上一稳定组合。
13. 连续下钻、关键环节切换、返回、场景切换和多实例运行没有持续内存增长、动画残留、粒子残留或迟到回调覆盖问题。
14. 第一层沙盘和第三层关键环节只显示占满子应用可视区的三维画面，第二层厂区同时显示三维画面与当前业务拓扑；三种模式切换无旧拓扑闪现、误交互或画布重建。
15. 所有美术、动画、粒子、映射、材质、相机位和性能验收证据均已记录在任务清单中。
