# 任务-037：三维反向选择与外部事件实施状态

> **2026-08-14 协议纠偏：** 三维反向选择目标改为静态 `sceneNodeId → nodeId` 唯一反查，外层不再接收设备编号。实例隔离、去重和回环阻断证据继续有效；主键迁移和新回归归任务-056。

**状态：** 待验收（90%）
**更新时间：** 2026-08-14

## 已实施

- `TopologyRegistry`（拓扑注册表）只从我方结构清单的二维节点读取可选 `sceneNodeId`（三维节点标识），建立“场景标识 + 三维节点标识 → nodeId”常数时间索引。平台内部绑定不携带三维节点；实现不根据 Unity 对象名称、层级、坐标、截图或字符串相同推导节点。
- `UnityObjectSelectionCoordinator`（Unity 对象选择协调器）只接受 `sceneId`（场景标识）、`sceneNodeId`（三维节点标识）和 `sceneActivationId`（物理场景激活标识）。它依次核对稳定上下文、唯一活动拓扑、场景、物理实例和显式设备映射，才同步二维节点与相邻连线。
- 物理场景激活标识由 `MultiSceneCoordinator`（多场景协调器）在每次真实场景提交或物理恢复提交时递增生成；同场景拓扑切换不改变它。该规则解决“燃气实例一 → 其他场景 → 燃气实例二”后，实例一迟到选择可能被实例二误接收的问题。
- Unity `objectSelected`（对象选择）与 `sceneChanged`（场景切换完成）都必须携带物理场景激活标识。内层协议仅允许对象选择载荷的三个字段，拒绝对象名称、场景状态、层级、路径、旧二维 `nodeId`（拓扑节点标识）和任意扩展字段。
- Unity 三维鼠标点击空白、地面或未映射对象时，若当前存在交互选择，则发送 `selectionCleared`（选择清除）事件；该载荷仅允许 `sceneId + sceneActivationId`，前端通过当前实例门禁清除二维节点与关联路径，不回发 `clearSelection`。
- `view.open`（原子打开视图）端口只接受已验证的场景激活标识；跨场景成功提交时记录新实例，同场景拓扑切换沿用旧实例，物理回退成功时才替换为恢复后的实例。
- 场景切换失败但 Unity 已自动恢复旧稳定场景时，失败 `commandResult`（命令结果）携带恢复后的新物理场景激活标识；前端恢复旧上下文时同步替换该标识，避免真实恢复后的新实例被当作旧实例。
- 同一内层消息标识通过固定 64 条先进先出队列在基础校验后立即登记。无映射、场景失配和实例失配事件同样只诊断一次，避免重复回放造成诊断洪泛或在后续稳定状态中被重试。
- 三维选择只提交来源为 `unity`（三维）的二维选择，绝不调用 `focusNode`（聚焦节点），因此不会形成 Unity → 二维 → Unity 的选择回环；三维选择清除同样只更新二维空选择，不回发 `clearSelection`。
- `HostRuntimeComposition`（宿主运行时组合根）在发送 `scene.object.selected`（三维对象选择）前再次核对握手、稳定场景、拓扑、上下文版本、`nodeId` 和非空二维节点，阻断绕过映射协调器的直接调用。
- 本地三层 Unity 模拟页同步升级为生成并回传物理场景激活标识；该夹具仅验证协议机制，绝不代表正式燃气设备资料。

## 本轮验证

- 前端专项回归覆盖显式映射、同一无映射消息去重、跨场景迟到、同名场景往返的旧实例阻断、新实例接受、同场景拓扑复用实例、恢复后激活标识替换、外层设备防御和内层完成关联。
- 2026-08-09 历史前端完整回归为39个测试文件、205项通过；类型检查与生产构建通过，生产构建转换95个模块。
- 2026-08-14 当前前端完整回归为43个测试文件、275项通过；类型检查和96模块生产构建通过。该全量结果覆盖最新夹具与状态边界，结构版本4网页图形构建证据仍按任务-051、038分别归档。
- 当前三维选择与选择清除专项为3个文件、29项通过；覆盖 `selectionCleared` 两字段校验、当前物理实例门禁、二维空选择提交、关联路径清除和不回发 Unity 命令。
- 代码质量：`git diff --check`（Git 差异空白检查）通过。
- 2026-08-09 历史 Unity 回归为编辑器模式22 / 22、播放模式9 / 9通过，直接覆盖对象选择三字段最小负载、强制同场景切换生成新物理激活标识及恢复后的活动实例约束，证据为 `artifacts/unity-editmode-results-20260809.xml` 与 `artifacts/unity-playmode-results-20260809.xml`。后续结构版本4构建的当前编辑器23 / 23、播放11 / 11证据归任务-051和任务-038，不重复计入本任务的反向选择闭环。
- 截至2026-08-09，内部浏览器上一轮三层夹具已证明显式合成映射、二维同步和不回发聚焦；由于对象选择协议新增物理场景激活标识，当时尚未重新执行夹具，因此旧浏览器结果不计作新 ABA（同值往返）保护的验证证据。
- 旧网页图形基线 `task051-final-20260805-2220` 的三个压缩产物确实未包含物理场景激活标识，不能与本任务新协议共同运行；该旧构建阻塞已由 `task051-v4-reverify-20260814-1100` 和 `task038-state-clear-v4-20260813-232422` 解除。结构版本4当前实例的浏览器反向选择已在本轮重新验证；旧实例和释放后迟到事件在下述第二轮浏览器回归中已闭合。真实燃气节点事件和状态链路仍须合作方独立服务验收。
- 2026-08-14 已在内置浏览器使用“外层宿主页 → 正式嵌入壳 → Unity 模拟页”重新执行结构版本4的当前实例反向选择。正常握手和燃气总览提交后，模拟页发出当前 `sceneActivationId`（物理场景激活标识）的 `objectSelected`（对象选择）；外层仅记录一次 `scene.object.selected`（三维对象选择），解析结果为合成 `nodeId` 和1个二维节点。模拟页受限 `focusNode`（聚焦节点）计数保持0，说明来源为Unity的选择未回发聚焦命令；同轮 `system.dispose`（系统释放）收到唯一已释放回执，控制台错误和警告均为0。该证据只覆盖当前有效实例的显式映射、二维选择提交、回环阻断和正常释放。
- 2026-08-14 第二轮内置浏览器回归新增受控准备步骤：外层宿主页只通过两次 `view.open`（原子打开视图）完成“燃气实例一 → 风电 → 燃气实例二”，不直连Unity；模拟页随后分别从可见按钮发送实例一、实例二和延迟选择。实例一的选择没有生成外层 `scene.object.selected`；实例二仅生成一次合成 `nodeId` 和1个二维节点的外层事件，`focusNode`计数保持0。安排一秒延迟选择后立即发送 `system.dispose`，最终仍只有前述一次对象选择和一次已释放回执，证明释放取消了待发选择计时器；控制台错误和警告均为0。该回归仅证明夹具、壳和协议机制，不替代合作方节点事件和真实状态验收。

## 未关闭事项与下一步

1. 正式燃气结构清单有已登记的二维→三维静态映射，`deviceMappings` 不属于清单模型。真实对象选择直接通过 `sceneNodeId → nodeId` 反查；平台内部设备绑定不参与三维映射。
2. 其余八个场景尚未交付正式三维节点、`doubleClickBehavior` 与二维拓扑结构映射；不得复用燃气测试映射。新业务节点统一使用 `emit-node`。
3. 由任务-040、049、052分别完成燃气结构冻结、单结构清单契约和真实三层验收；本任务不得替平台猜测真实设备绑定。

兼容网页图形构建、结构版本4当前实例反向选择、旧实例拒绝、新实例一次上报和释放后延迟选择阻断均已完成本地浏览器验证。剩余合作方节点事件、节点状态和真实三层验收属于外部环境验证，任务保持待验收90%；在真实联调证据到位前不得标记完成。
