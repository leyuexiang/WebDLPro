# 任务-039：燃煤发电场景配置与映射实施状态

## 当前状态

- 状态：进行中（45%）
- 启动日期：2026-08-05
- 当前阶段：Unity 模型已导入，场景控制器和属性面板绑定已建立；前端燃煤拓扑、动作和三层联调仍未完成。
- 结论：`CoalPower.unity` 已不再是空占位，当前可作为燃煤三维场景和显式模型绑定的开发基线；它仍不能单独证明前端拓扑、平台设备主数据或完整发布验收已完成。

## 已证实的现状

| 项目 | 已证实事实 | 直接依据 | 本任务可使用的范围 | 不可推断的范围 |
| --- | --- | --- | --- | --- |
| 稳定场景标识 | `coal-power` 已在固定九场景目录中登记 | `BusinessSceneCatalog`、前端场景标识类型 | 外层请求、发布清单的固定场景槽位 | 真实 Unity 对象、设备或流程关系 |
| Unity 文件 | `Assets/Scenes/Business/CoalPower.unity` 已包含 `SceneRoot/Equipment`、导入模型、`PowerPlantRuntime` 和 `PowerPlantProcessController` | Unity 文本序列化内容 | 场景路径、控制器加载和显式对象引用 | 前端拓扑、平台设备主数据或完整发布资源 |
| 场景能力 | 控制器从属性面板读取 `coal-power-generation`、节点绑定和三态四态目标；路径流仍明确不支持 | `PowerPlantProcessController.cs`、`CoalPowerBusinessSceneControllerAdapter.cs`、场景序列化字段 | 初始化、流程步骤、节点聚焦、显隐、重置、释放和已登记模型的四态更新 | 前端燃煤拓扑、设备双击平台绑定、三维动态路径 |
| 前端工艺文案 | 仅有燃煤页面标题与工艺域标识 | `local-process-config.ts` | 页面或外层业务文案的稳定标识 | 默认拓扑、节点、设备主数据或三维映射 |
| 架构资料 | 燃煤明确标为“待交付 Unity 场景、拓扑和动作映射” | `电力全流程平台-前端架构设计.md` | 作为不猜测的边界依据 | 用通用燃煤工艺知识补齐业务数据 |

## 未收到且必须提供的正式资料

本任务需要以下资料同时到位后，才可开始写入真实清单或 Unity 内容：

1. Unity 燃煤场景交付物：稳定 `unitySceneKey`、资源版本、场景资源预算、加载策略、失败与降级策略，以及场景中可对外暴露的 `sceneNodeId` 清单。
2. 默认拓扑与全部可切换拓扑：每个拓扑的正式 `topologyId`、节点、连线、二维布局、图元键和状态基线。
3. 节点事件与结构映射：每个业务源拓扑节点提供稳定 `nodeId`、可选 `sceneNodeId` 和 `doubleClickBehavior: 'emit-node'`；非业务说明、分组和层级标题不得作为伪业务节点进入清单。结构清单不得写入设备编号或绑定映射，平台读取 `nodeId` 后在平台内部完成真实设备绑定。
4. 动作与流程映射：外部 `actionId`、目标拓扑、流程 `processId + stepId`、节点聚焦、路径和失败策略；不接受菜单文案或模型名字代替标识。
5. 验收与版本资料：场景、拓扑、动作、结构和 Unity 映射版本必须作为同一基础发布版本原子交付；平台内部设备绑定变化不修改我方结构清单，也不要求重载壳。只有我方结构版本变化时才重新加载完整结构清单，并提供单元、Unity编辑器及三层联调的可复现验收条件。

## 已完成的 Unity 属性面板配置

燃煤绑定与燃气使用同一套 `PowerPlantProcessController` 序列化字段，运行时不按模型名称查找。当前场景保存了以下稳定节点与对象引用：

| sceneNodeId | 属性面板目标 | 允许的视觉状态 |
| --- | --- | --- |
| `plant.overview` | 锅炉、汽包、下降管、汽轮机、发电机、励磁机的总览目标 | 仅显隐上下文，不作为状态绑定 |
| `node.coal-boiler` | `燃煤锅炉2`、`汽包`、`下降管1` | 红色模型，允许聚焦和四态 |
| `node.coal-steam-turbine` | `低中高压汽轮机` | 红色模型，允许聚焦和四态 |
| `node.coal-generator` | `发电机`、`励磁机` | 红色模型，允许聚焦和四态 |

黑色系统、无模型现场设备和资料中没有独立模型的 SIS 逻辑联锁不填入绑定数组。`PowerPlantProcessControllerEditor` 只做空引用、重复标识和渲染器检查，不会按对象名称自动补写绑定。

## 当前保护性结论

1. 不会把 `CoalPower.unity` 的文件名、层级名称或六个燃煤页面名称自动解释为业务节点；只有属性面板中显式保存的 `sceneNodeId` 和对象引用才生效。
2. Unity 燃煤场景可以完成初始化和已登记流程动作，但前端清单仍未提供正式燃煤拓扑、节点事件、动作和平台内部设备绑定，因此不能宣称燃煤全流程交付。
3. 不会复制 0813 燃气基线的 23 节点/22 连线总图、三个三维聚焦映射或四个受控动作到燃煤场景。燃气模板只复用清单结构、布局路由算法、交互协议、验证规则和“不猜测”原则，不复用燃气业务数据。

## 下一动作与验收门槛

收到上述资料后，按以下顺序实施并更新本文件：

1. 为燃煤页面建立正式总拓扑和三个关键环节子拓扑，固定 `nodeId`、布局、连线和状态基线。
2. 将前端 `nodeId` 与属性面板中的 `sceneNodeId` 显式登记，补齐动作、流程步骤和失败策略；不写入设备编号。
3. 逐项验证 Unity 初始进入、同场景子项、跨场景进入、拓扑单击、节点双击、状态更新、失败恢复和资源释放。
4. 只有每一项都具备代码、正式资料和三层联调证据后，才可结束任务-039。
