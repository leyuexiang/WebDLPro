# 任务-049：清单与动作映射契约测试实施状态

**状态：** 进行中（80%）
**更新时间：** 2026-08-05

## 已交付的发布门禁

新增 `power-data-web/scripts/validate-scene-topology-manifest.mjs`（场景拓扑清单校验命令）。它通过 Vite（前端构建工具）的服务端模块加载器复用运行时同一份 `validateSceneTopologyManifest`（场景拓扑清单校验器），不会复制或漂移校验规则。

```powershell
cd F:\WorkSpace\WebDLPro\power-data-web
npm run validate:manifest -- --manifest <工作区内正式清单.json> --report <工作区内既有目录/契约报告.json>
npm run build:release -- --manifest <工作区内正式清单.json> --report <工作区内既有目录/契约报告.json>
```

`validate:manifest`（清单校验）输出 UTF-8（统一码转换格式）JSON（JavaScript 对象表示法）报告，且只记录工作区相对路径、稳定问题代码和脱敏中文摘要；不输出原始清单、绝对路径或异常堆栈。`build:release`（发布构建）必须先执行同一校验，只有报告有效时才运行类型检查和生产构建。

| 结果 | 退出码 | 发布行为 |
| --- | ---: | --- |
| 清单有效 | 0 | 生成有效报告；`build:release`继续执行生产构建 |
| 清单契约无效 | 1 | 生成问题报告；`build:release`在构建前停止 |
| 参数、路径、读取、解析、报告写入或校验器加载失败 | 2 | 停止；不把未确认清单交给构建 |

命令只接受工作区内的相对路径。报告父目录必须由发布流水线预先创建；命令不会创建或覆盖工作区外文件。

## 契约覆盖范围

复用的校验器会阻断下列发布缺口：

1. 九个固定场景缺失、重复或默认拓扑跨场景。
2. 场景、拓扑、动作、设备映射和 Unity 场景映射版本不一致。
3. 多拓扑归属错误、拓扑节点或连线的孤立引用、场景动作目标错误。
4. 动作引用未登记的三维节点、路径或流程步骤。
5. 双击设备节点缺少 `deviceId`（设备标识）、设备映射遗漏二维节点、二维和三维节点映射不一致。
6. 三维节点未在所属 Unity 场景映射中登记，以及同一三维节点由多个设备状态源驱动。

## 持续集成接入

新增 `.github/workflows/power-data-web-contract.yml`（GitHub Actions 持续集成工作流）。仓库远端已核对为 GitHub，因此本次接入不依赖平台猜测。工作流只授予 `contents: read`（仓库内容只读）权限，15 分钟超时，并对同一引用的新提交取消旧任务，避免过时构建继续占用执行器。

| 触发方式 | 清单来源 | 执行内容 | 归档内容 |
| --- | --- | --- | --- |
| 推送、拉取请求且修改前端或工作流 | 已提交的完整合成九场景夹具 | 安装锁定依赖、35 组单元与命令测试、双容器视觉门禁、清单校验和发布构建 | 仅 14 天有限 JSON 契约报告 |
| 手工触发 | 输入的工作区内相对清单路径，默认同一夹具 | 同上；路径继续由现有命令拒绝绝对路径和越界访问 | 同上 |

工作流不会上传原始清单、Unity 资源、网页图形构建产物、测试缓存或令牌。报告目录由流水线显式创建；`build:release`（发布构建）仍先调用既有校验器，清单无效时不会启动生产构建。

## 验证证据

- `tests/fixtures/scene-topology-contract-valid.json`（完整九场景合成清单）验证命令输出有效报告，且 `build:release`成功完成类型检查和生产构建。
- `tests/fixtures/scene-topology-contract-invalid.json`（缺失九场景的错误清单）验证命令与 `build:release`均返回退出码 1，且生产构建不会启动。
- `tests/manifest-contract-command.spec.ts`（命令行门禁测试）覆盖有效报告、报告文件归档、无效问题代码和发布构建前阻断。
- 前端完整单元回归：35 个测试文件、154 项测试通过；类型检查、版本化视觉门禁与有效清单发布构建通过。
- 本地按工作流相同命令验证：`npm run test:unit`、`npm run test:visual`、`npm run build:release -- --manifest tests/fixtures/scene-topology-contract-valid.json --report artifacts/scene-topology-contract-report.local.json` 全部通过，并生成有效的有限 JSON 报告；验证后已删除本地临时报告。
- 本地 `npm ci --ignore-scripts` 在 Windows 因用户常驻 4173、4175 开发服务锁定 Vite 原生绑定而中断；未终止用户进程，已用同一锁文件恢复本地依赖。GitHub 托管执行器使用干净工作目录，不继承该锁定状态；工作流首次推送后的远端运行仍需实际确认。

以上夹具仅用于验证门禁机制，不代表任务-039—047的正式业务映射。

## 尚未关闭项

1. 未收到正式九场景清单、动作、设备、流程、路径和资源版本资料，无法对真实发布物生成报告。
2. GitHub Actions 工作流已接入源码，但尚未推送触发首次远端运行；首次运行后需确认上传的报告与失败退出码均符合预期。
3. 真实 Unity 网页图形构建及九场景资产尚未完成，不能把合成夹具的构建通过计为正式发布验收。

## 结论

任务-049的可执行契约命令、结构化报告、GitHub Actions 门禁和“无效清单阻断发布构建”机制已经就绪，进度更新为 80%。正式清单到位后，须在首次远端工作流中运行同一命令生成真实报告，并完成 Unity 网页图形构建验收，才能进入最终验收。
