# 任务-034：同场景流程动作实施状态

## 当前状态

- 状态：已完成（100%）
- 已完成：外层 `workflow.trigger`（流程触发）能力按实际安装状态发布；受控流程处理器、同场景动作到原子 `view.open` 事务的复用、动作失败策略、稳定视图变更上报，以及动作阶段的取代与错误回归。
- 关闭依据：任务-034交付同场景流程动作机制，不负责猜测或补造正式业务动作。正式动作参数到 Unity 白名单字段的映射、逐场景动作清单和真实网页图形业务验收分别由任务-039—047、049与052收口。

## 实现规则

1. 流程触发只接受当前原子清单已登记的 `actionId`；目标场景不是当前稳定场景时明确拒绝，留给任务-035的跨场景路径处理。
2. 同场景动作复用唯一原子切换处理器，因此不发送 `switchScene`、不释放 Unity 运行实例，也不创建第二个拓扑画布。
3. 动作成功后才激活其显式目标拓扑并递增一次上下文版本。
4. `keep-current-context`（保持当前上下文）是默认失败策略：动作失败后保持上一稳定场景与拓扑，且不激活目标拓扑。
5. 只有清单显式声明 `commit-view-with-warning`（带警告提交视图）时，动作失败才继续提交映射拓扑，并保留受控失败诊断。
6. 外层参数名称必须是当前动作允许的稳定标识；由于当前资料未提供“参数到 Unity 字段”的显式映射，任何非空参数都会被结构化拒绝，绝不静默忽略或猜测写入位置。

## 本次交付

| 文件 | 作用 |
| --- | --- |
| `power-data-web/src/modules/visual/orchestration/workflow-trigger-transaction-handler.ts` | 校验当前稳定上下文、动作、目标场景和参数后，复用原子打开视图处理器执行同场景动作。 |
| `power-data-web/src/modules/visual/orchestration/view-open-transaction-handler.ts` | 补齐同场景动作的两种显式失败策略；跨场景失败仍保留物理回退路径。 |
| `power-data-web/src/host-bridge/host-runtime-composition.ts` | 仅在流程处理器已注入时声明并接收流程触发能力；成功后只派生一次稳定视图变更。 |
| `power-data-web/src/app/EmbeddedVisualizationShell.vue` | 在唯一组合根创建流程处理器，不向其暴露 iframe、释放端口或状态仓库写端口。 |
| `power-data-web/src/modules/visual/orchestration/workflow-trigger-capability.ts` | 以清单动作数量为常数时间能力门禁；空动作清单不创建流程路由，也不向父页面发布无法执行的 `workflow.trigger`。 |
| `power-data-web/tests/local-shell-regression/*` | 合成燃气重置动作由总览切至空明细拓扑，供三层回归验证“动作成功后才切图”；不作为正式业务配置。 |

## 验证结果

- 专项单元测试：2026-08-08 实际执行 `view-open-transaction-handler.spec.ts`、`workflow-trigger-capability.spec.ts` 与 `host-runtime-composition.spec.ts`，共 3 个测试文件、35 项通过。覆盖空动作清单不发布能力、已初始化后伪造流程命令的未声明能力拒绝、总览→明细拓扑的同场景动作、无 Unity 动作映射、过期上下文版本拒绝、跨场景误路由拒绝、默认失败保持上下文与显式警告提交。
- 全量前端单元测试：2026-08-08 实际执行 `npm run test:unit`，37 个测试文件、191 项通过。
- 类型检查与生产构建：`npm run build` 通过；本地三层夹具服务脚本已通过 `node --check`。
- 内部浏览器：外层测试宿主页触发合成 `action.gas.reset` 后，收到关联完成结果和版本 2 的 `view.changed`（视图变更）；拓扑从燃气总览切至燃气明细，Unity 模拟页确认 `resetScene`（重置场景），Unity 内嵌框架地址不变、未走 `switchScene`（切换场景）路径且无控制台错误。

## 后续任务交接

1. 目前只有合成燃气重置动作；正式九场景的动作、参数字段、失败策略与目标拓扑映射由任务-039—047和049交付。当前拒绝非空流程参数，直到正式清单提供“参数标识 → Unity 受控字段”的逐项映射。
2. 真实网页图形的逐场景流程动作回执、性能和用户验收由任务-052及053完成；当前真实燃气发布包无宿主流程动作时，能力门禁不会向父页面错误发布 `workflow.trigger`。
