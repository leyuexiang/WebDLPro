# 任务-021：流程、聚焦、状态和路径动作适配实施状态

## 当前结论

任务-021已阻塞，完成度为 60%。内层网页图形协议已具备流程、聚焦、四态状态、路径流动、显隐和复位的受控命令边界；所有命令只传递稳定业务标识，经过前端与 Unity 双重校验后才会进入当前业务场景控制器。

燃气发电场景已实际复用流程、聚焦、显隐和复位能力。四态状态和路径流动没有正式映射或资源交付，燃气控制器明确返回 `capability-unsupported`，不将既有告警高亮、静态流动或模型名称猜测为正式四态和路径功能。

## 已交付内容

- `power-data-web/src/services/webgl/protocol.ts`
  - 将 `setNodeVisualState`、`setRouteFlow` 纳入命令白名单。
  - 定义流程、三维节点、四态状态、路径和显隐的受控载荷与运行时守卫。
  - 聚焦、状态和显隐统一使用 `sceneNodeId`，不再用二维 `nodeId` 隐式替代。
- `power-data-web/src/services/webgl/runtime-connector.ts`
  - 在创建有限待确认记录前校验场景动作载荷；无效载荷不会进入 iframe 或占用命令容量。
  - 为每种动作返回不泄露原始载荷的稳定失败说明。
- `Assets/Scripts/UnityIframeBridgeManager.cs`
  - 将六类动作分派到当前 `IBusinessSceneController`。
  - 对流程、三维节点、路径和四态执行独立校验；状态只转换为固定枚举，桥接不接收颜色、材质、层级或资源参数。
  - 路径速度不由网页端控制，统一使用场景内部基线值，避免异常倍速造成动画开销失控。
- `Assets/Scripts/Visualization/Scenes/SceneSwitchProtocolModels.cs`
  - 增加纯数据动作校验器与四态转换器，避免协议判断依赖场景对象。
- `Assets/Plugins/WebGL/Power3dUnityBridge.jslib`、运行时登记表及本地三层夹具
  - 同步更新能力声明；夹具分别验证载荷形态、能力协商、原请求回执与待确认清理。

## 已验证证据

- 前端类型检查通过。
- 前端单元测试：20 个文件、79 项通过。
- 前端生产构建通过。
- Unity 编辑模式：15 项通过，结果为 `artifacts/unity-tests/editmode-task-021.xml`。
- 内部浏览器本地三层夹具通过：流程、聚焦、四态状态、路径、显隐和复位均以独立原请求标识收到确认；控制台无错误。

## 尚未完成的验收项

1. 新增的 Unity 播放模式动作回归尚未执行。已连续三次检测到用户当前打开的 Unity 编辑器持有同一项目锁，批处理测试无法并行启动；关闭该编辑器后需运行 `artifacts/unity-tests/playmode-task-021.xml` 并记录结果。
2. 燃气场景的正式四态状态映射、路径映射及对应资源尚未提供。任务-040的燃气模板冻结与正式动作映射交付后，才可实现并验证材质或材质属性复用，以及未知节点、未知路径的场景级错误。
3. 其余八个业务场景仍是显式内容未交付占位场景，命令会返回结构化“不支持”或“内容未交付”，不能伪造九场景动作能力。

## 关闭条件

关闭任务-021前必须满足以下条件：

1. Unity 播放模式动作回归通过，覆盖有效流程、未知流程、无效三维节点、未声明四态/路径能力和复位回执。
2. 正式燃气四态与路径映射资料到位后，验证状态更新复用材质或材质属性，不为每次更新克隆资源。
3. 正式场景映射发布后，验证不存在的节点和路径均返回场景级明确错误码。
