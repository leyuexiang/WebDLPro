# 任务-028：拓扑单击与三维聚焦实施状态

**状态：** 已完成（100%）
**更新时间：** 2026-08-13

## 完成结论

任务-028已按当前验收口径关闭：二维拓扑单击立即提交本地节点与关联路径选择；仅在当前稳定场景、当前活动拓扑、显式 `sceneNodeId`（三维节点标识）映射和已协商 `focusNode`（聚焦节点）能力同时满足时，异步请求 Unity 聚焦。拓扑选择和 Unity 鼠标命中已登记模型都会更新三维交互描边，并共用 Unity 场景控制器的 `_focusOnSelection` 自动聚焦开关。三维失败不撤销二维选择，同一 `selectionId`（选择标识）在前端和 Unity 两端均只执行一次，Unity 来源不会反向触发聚焦回写。拓扑空白点击仍通过下行 `clearSelection` 清除三维交互描边；Unity 鼠标空白、地面或未映射对象点击则通过上行 `selectionCleared` 清除二维节点和连线，两条方向均不重置场景上下文。

## 已实施

### 空白点击同步清除三维描边

当前存在两条互不回环的空白清除方向：拓扑空白点击下发 `clearSelection`，Unity 三维空白点击上行 `selectionCleared`。

拓扑空白点击现在会先提交空的二维选择并清除二维关联路径，再通过独立的 `clearSelection`（清除选择）命令通知 Unity 关闭当前交互选中描边。Unity 三维鼠标点击空白、地面或未映射对象时，如果存在交互选择，则清除交互描边并发送 `selectionCleared(sceneId, sceneActivationId)`；前端通过当前场景实例门禁清除二维选择，不回发 Unity 命令。两条链路都不复用 `resetScene`（场景重置），不改变当前流程、模型显隐、告警描边或已经到达的镜头位置。

前端协议、运行时能力协商、Unity 浏览器桥、业务控制器适配器和燃气场景控制器均已登记该命令；能力未协商或场景不支持时明确拒绝，不静默执行。

正式网页模板和本地三层回归夹具也已同步 `clearSelection` 能力。Unity 构建元数据从结构版本2升级为结构版本3，并写入完整下行命令能力清单；发布脚本会在复制包前拒绝缺少该能力的旧 Unity 包，避免正式握手阶段才暴露兼容问题。

1. `TopologySelectionFocusCoordinator`（拓扑选择聚焦协调器）只接收选择来源、品牌化 `selectionId` 和清单显式声明的 `sceneNodeId`，不接触画布、内嵌框架、窗口对象、模型名称、层级路径或坐标。
2. 拓扑单击先经唯一可视化协调器提交二维节点与关联路径，再同步活动画布；只有提交成功后才异步请求三维聚焦。聚焦失败只返回失败结果，不回滚已经提交的二维描边、路径或选择状态。
3. 当前运行时不是就绪态、画布回调不属于当前稳定场景/拓扑、选择未变化、节点无显式三维映射或 Unity 未声明聚焦能力时均无副作用，不根据标题、图元、坐标、文件名或资源名称猜测三维对象。
4. 新增独立 `SelectionId`（选择标识）品牌类型及工厂。每次已接受的拓扑选择生成一个不超过128字符的稳定选择标识，并随 `focusNode` 载荷发送；`view.open`（视图打开）中的聚焦动作则把其唯一事务标识显式转换为选择标识，禁止类型层隐式混用。
5. 网页图形协议将聚焦载荷固定为 `sceneNodeId + selectionId + isolate`（三维节点、选择标识、隔离开关）。前端连接器、Unity 桥接器、本地 Unity 模拟页和开发测试面板均拒绝缺少选择标识的旧载荷。
6. 前端协调器使用固定容量64的选择标识映射表，在调用三维端口前登记；聚焦失败后重放同一选择也不会再次发送。组件销毁时显式清空去重表。
7. Unity 桥接器使用固定容量64的队列与哈希集合进行常数时间查重。同一选择即使换用新的消息标识重发，也只返回幂等成功结果，不再次触发相机补间、模型描边或控制器调用；释放与销毁时同步清空两个集合。
8. `unity`（Unity 运行时）来源被拓扑聚焦协调器直接阻断。三维反向选择只同步二维选择与受控外层事件，不会形成二维→三维→二维循环。
9. 双击继续复用当前活动拓扑内已命中的 `nodeId`（二维节点标识）；双击自身不重复提交选择或发送聚焦，派生的重复单击由当前选择和选择标识幂等边界共同阻断。

## 变更文件

- `power-data-web/src/config/scene-topology/identifiers.ts`及测试：新增独立选择标识类型、工厂和不可互换编译期契约。
- `power-data-web/src/modules/visual/topology/topology-selection-focus-coordinator.ts`及测试：选择标识透传、固定容量去重、失败隔离和释放清理。
- `power-data-web/src/modules/visual/topology/ManifestTopologyRuntimePanel.vue`：当前稳定上下文门禁、聚焦载荷组装和组件释放。
- `power-data-web/src/services/webgl/protocol.ts`、`runtime-connector.ts`及测试：聚焦载荷三字段强校验和明确错误说明。
- `power-data-web/src/modules/visual/runtime/visualization-runtime-view-open-port.ts`及测试：动作事务到选择标识的显式转换。
- `Assets/Scripts/Visualization/Scenes/SceneSwitchProtocolModels.cs`：选择标识统一边界校验。
- `Assets/Scripts/UnityIframeBridgeManager.cs`：聚焦选择固定容量幂等表与释放清理。
- `Assets/Scripts/PowerPlant/PowerPlantRuntimeTestPanel.cs`：开发桥接测试改用正式三维节点和选择标识字段。
- `Assets/Tests/EditMode/BusinessSceneRuntimeTests.cs`、`Assets/Tests/PlayMode/MultiSceneCoordinatorPlayModeTests.cs`：选择标识校验与重复聚焦回归。
- `power-data-web/tests/local-iframe-regression/unity-mock/index.html`、`host/index.html`：本地三层夹具同步新版聚焦载荷和清除交互描边命令。
- `Assets/WebGLTemplates/EmbeddedViewport/index.html`、`Assets/Plugins/WebGL/Power3dUnityBridge.jslib`：正式网页模板与 WebGL（网页图形）桥接白名单同步 `clearSelection`。
- `Assets/Editor/PowerPlantWebGlBuild.cs`、`Assets/Scripts/Visualization/Scenes/SceneSwitchProtocolModels.cs`、`power-data-web/scripts/build-gas-power-smoke-release.mjs`：构建元数据命令能力清单和旧包发布门禁。

## 验证证据

- 前端全量回归：40个测试文件、230项测试全部通过。
- 前端类型检查与生产构建通过；本轮新增文件的 `git diff --check` 未发现空白错误（工作区其他历史用户修改仍保留原有尾随空格）。
- Unity编辑模式：22 / 22通过。
- Unity运行模式：11 / 11通过。专项用例使用不同 `messageId`（消息标识）重发同一 `selectionId`（选择标识）时，控制器聚焦调用仍为1次；新增 `clearSelection` 命令后，活动控制器清除调用为1次，且与 `resetScene`（场景重置）分派独立。
- 内置浏览器三层夹具：动作回归先收到 `focusNode` 再收到独立 `clearSelection`，两条命令均成功回执；页面显示“清除交互描边”通过，控制台无错误或警告。模拟页不保存场景、镜头或模型状态，因此该回归专门验证命令能力、载荷和 `requestId`（原请求标识）关联。
- 用户已在真实燃气测试包中人工确认：点击已登记拓扑节点后，Unity 三维模型能够聚焦并显示描边。
- 正式Unity网页图形 `task064-clear-selection-20260813-1610` 已生成结构版本3元数据，并由燃气联调包 `gas-power-smoke-clear-selection-5544-20260813` 完成真实网页回归：点选“燃机 Mark VIe 控制器”后，Unity聚焦燃机并显示青色交互描边；点击拓扑真正空白区域后，二维选中和三维交互描边均消失，镜头仍保持聚焦位置。截图为 `artifacts/browser-tests/task064-gas-node-focus-5544-20260813.png` 和 `artifacts/browser-tests/task064-gas-clear-selection-verified-5544-20260813.png`。
- 当前交付复核统一指向 `Builds/Releases/燃气发电场景与拓扑图联动包0813`：其总览拓扑仍保留上述 `focusNode → clearSelection` 行为，节点聚焦和空白清除均不依赖内部自测按钮。5544仅保留为该交互首次形成的历史截图证据。

## 性能与释放

- 单击查找节点为当前拓扑节点线性查找，关联路径为当前拓扑边单次扫描；不增加嵌套全图查询或隐藏画布。
- 前端和 Unity 的去重历史均固定上限64；前端不保存未结算承诺，Unity 不保存命令载荷、控制器引用或异步对象。
- 组件、桥接释放和对象销毁均有显式清理路径，不新增常驻监听器、计时器、材质实例或动画循环。

## 范围边界

- 任务-028只关闭单击选择、空白清除交互描边、显式映射聚焦、幂等、失败隔离和回环阻断框架。九场景正式 `sceneNodeId`、设备、动作和资源内容由任务-039—047逐场景交付，不能回退为本任务阻塞项。
- 当前真实业务证据覆盖用户已确认的燃气已登记节点；无映射节点仍只执行二维选择，不猜测三维目标。
- 三维反向选择与外层事件归任务-037；其现有实现已通过来源阻断验证，本任务不重复扩展该职责。

## 结论

任务-028已满足“二维立即选中、空白点击只取消三维交互描边、显式映射才聚焦、三维失败不回滚、同一选择只执行一次、Unity来源不回写、释放无残留”的全部要求，状态更新为已完成（100%）。结构版本3正式Unity包与真实燃气网页回归均已补齐；后续重构建仍必须保留相同元数据门禁和交互回归。下一项按依赖关系收口任务-029。
