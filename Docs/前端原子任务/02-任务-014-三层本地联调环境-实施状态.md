# 任务-014：三层本地联调环境实施状态

> **2026-08-15 协议偏离说明：** 以下分阶段记录包含旧“运行时清单、设备编号注入、绑定计数”夹具证据，统一保留为历史回归基线。任务-056已完成结构清单、`nodeId` 双击和节点状态的本地三层迁移与浏览器复验；合作方独立服务验收未完成，因此不能用本地夹具关闭任务-014。

**状态：** 进行中（80%；当前协议本地迁移完成，合作方环境待验收）
**最后更新：** 2026-08-15

## 已完成的保护性基础

1. 新增 `power-data-web/tests/local-shell-regression`，提供测试专用“外层宿主页 → 当前嵌入壳 → Unity 模拟页”链路；现有两层 `local-iframe-regression` 保持不变。
2. 测试宿主先注册监听器，再加载嵌入壳；它只通过 `power-scene-topology-shell`（外层协议通道）发送 `system.init`（系统初始化）、`state.get`（状态查询）、`device.states.update`（设备状态更新）、`workflow.trigger`（流程触发）和 `system.dispose`（系统释放），不直连 Unity。
3. 测试服务动态提供合成九场景结构清单，仅燃气场景带两枚节点双击节点；清单不含正式资源路径、业务动作或生产设备资料。第二枚节点仅用于验证完整快照缺失节点时的恢复和三维清除。
4. 内置浏览器已验证三层 iframe 层级实际建立，并发现并修复部署配置中把外层宿主页来源错误复用于 Unity 直接父页面来源的问题：现使用独立 `VITE_POWER_UNITY_PARENT_ORIGIN`（Unity 直接父页面来源）并保持精确来源校验。
5. 浏览器实测已通过：`system.ready → system.init → system.ack`（系统就绪、初始化、确认）、初始原子 `view.open`（打开视图）、Unity 模拟场景最终完成事件、`view.changed`（视图变更）、`state.get → state.snapshot`（状态查询与快照关联）和燃气测试节点的 `topology.node.dblclick`（拓扑节点双击上报）。
6. 修复 `system.dispose`（系统释放）过早确认问题：外层 `command.result: disposed`（命令结果：已释放）现在等待 Unity 宿主清理 iframe、连接器、观察器及待确认表后发送；浏览器实测回包后内层 iframe 数量为零，并显示“运行时已释放”。
7. 测试宿主页新增“执行基础三层回归”：已验证状态快照与命令结果关联、旧会话拒绝、同源非父窗口拒绝和释放确认；同一 `messageId`（消息标识）重复发送后只保留首条业务结果，重复消息静默丢弃且不重复派生状态快照或其他副作用。
8. 测试宿主页新增“执行超时与恢复回归”：风电短延迟验证新燃气事务立即取代旧事务；配电最终失败回执验证上一稳定燃气上下文恢复；光伏延迟超过十秒验证外层 `command.timeout`（命令超时）及迟到最终完成回调无法覆盖恢复事务；内置浏览器实测全部通过且最终释放后内层 iframe 数量为零。
9. 连续完整快照回归已迁移为节点协议：首批发送两个合成节点告警，后批使用较小来源修订号仅保留燃机节点，证明缺失节点会恢复二维配置基线并向 Unity 模拟页发送独立清除命令；该证据仍需在合作方独立服务环境复核。

## 2026-08-14 清单缓存响应策略对齐

- 本地夹具的 `/manifest.json` 现在返回 `Cache-Control: no-cache, max-age=0, must-revalidate`（禁止缓存并要求重新验证），与生产联调服务及嵌入壳的响应头门禁一致；测试宿主页和错误响应继续使用 `no-store`（不存储）。
- 这样可以在本地三层回归中真实覆盖“结构清单版本变化后重新加载”的缓存边界；平台内部设备绑定不改变我方清单，也不触发我方清单刷新。
- 该调整只修正联调夹具响应头，不改变生产通信协议、合成清单内容或任务完成条件；当时未提高任务-014进度，合作方读取结构清单和真实模型状态证据仍待提供。

## 2026-08-14 本地状态快照回归证据

- 内置浏览器真实执行“外层宿主 → 嵌入壳 → Unity 模拟页”连续完整快照回归，外层收到两份且各一次的 `command.result: completed`（命令完成）。第一批两个节点均为告警，第二批仅保留燃机节点并将其改为故障；二维状态按到达顺序整体替换，较小来源修订号未阻止后到快照覆盖。
- Unity 模拟页受限诊断最终为：设置视觉状态命令 `3` 次、清除视觉状态命令 `1` 次、最后设置状态为 `fault`（故障）、活动动态状态数量 `1`。这证明缺失的发电机节点被清除，燃机节点故障仍保留；诊断属性只存在本地夹具，未进入平台交付包。
- 同轮基础三层回归通过：查询关联、重复 `messageId`（消息标识）静默丢弃、旧会话拒绝、伪造来源拒绝和释放确认均通过。正式协议单测明确拒绝 `items: []`，同时保留 `1—500` 项通过和 `501` 项整体拒绝。
- 同一浏览器会话随后执行释放，再以夹具专用代次参数重新加载同一宿主页并复跑完整快照。新会话重新收到 `system.ready`（系统就绪）、`system.ack`（系统确认）和 `view.changed`（视图变更）；两批状态各仅产生一次二维成功结果，Unity 模拟页最终仍为设置 `3` 次、清除 `1` 次和仅燃机故障。该参数只触发本地测试 iframe 重新导航，不改变实例、会话、来源或正式协议。

## 2026-08-14 单在途状态重试回归证据

- 新增测试专用父页面控制器：每五秒检查一次最新完整快照；存在未完成 `device.states.update`（设备状态更新）时只记录跳过，不发送第二条命令。
- 内置浏览器实际穿过“外层宿主页 → 嵌入壳 → Unity 模拟页”运行约十五秒回归。宿主页按计划忽略首个状态确认，五秒后仅更新数据源，十秒截止后下一次检查重新读取较小来源修订号的最新非空完整快照，并使用新的 `messageId`（消息标识）发送；第二条命令收到一次二维成功回执。
- 控制器自动契约测试以真实 `command.result: disposed`（命令结果：已释放）对象驱动释放入口，验证其会同步清理轮询和十秒计时器，旧会话不会在释放后生成重试；宿主页收到实际释放结果时复用同一入口。内置浏览器进一步验证状态命令在途时发送 `system.dispose`（系统释放），外层收到唯一释放回执后超过十秒仍不产生第二次状态发送，回归显示“已随系统释放停止”而非超时失败。该测试控制器及页面按钮只在 `tests/local-shell-regression` 下提供，不进入生产构建、平台包或正式清单。
- 该回归模拟的是父页面确认丢失，不伪造合作方真实节点状态，也不能替代合作方平台实际五秒检查和网络异常验收。

## 2026-08-14 显式不可恢复失败重试边界

- 本地父页面控制器现区分可恢复结束与显式不可恢复失败。收到 `success: false` 且 `recoverable: false`（不可恢复）的状态命令结果后，仅保存该份完整状态内容的受限指纹；后续五秒检查读取到相同状态内容时保持静默，不能只替换消息标识或来源修订号就重发。
- 当最新完整快照的节点状态内容发生变化时，控制器才解除抑制、重新读取该快照并生成新消息标识发送。定向契约测试同时覆盖来源修订号变化不绕过门禁、状态变化后重新发送，以及释放结果停止轮询和十秒计时器。
- 这是对正式协议“显式不可恢复失败不得重发相同载荷”的本地父页面遵循性验证，不改变嵌入壳生产代码，也不能替代合作方平台的真实网络失败和节点状态联调。

## 2026-08-14 清单接口合同夹具

- 测试服务保留默认 `/manifest.json` 双节点结构清单；历史路径 `/manifest-empty.json` 当前返回等价结构清单副本，另提供 `/missing-package/manifest.json` 联动包不存在404和 `/missing-file/manifest.json` 清单文件缺失404。
- 独立端口合同测试验证结构清单不含设备标识、设备映射或绑定计数，且两类404正文使用合作方约定的固定错误标识；测试结束后端口无残留监听。
- 合同测试进一步通过正式 `RemoteSceneTopologyManifestLoader`（远程清单读取器）实际读取三个地址，确认结构清单进入运行时、两类404转换为稳定错误码；未读取或透传服务端原始正文。
- 该夹具只证明读取器和本地服务的协议边界，不能替代合作方真实结构清单读取、平台内部绑定或节点状态验收；该阶段任务-014维持75%。

## 2026-08-14 状态边界三层回归

- 新增“执行状态边界三层回归”。它先通过正式 `system.dispose`（系统释放）释放旧夹具会话并重建壳，再发送一份合法非空完整快照，其中同一合成燃气轮机节点依次为告警、故障。父页面只收到一份 `command.result: completed`（命令结果：完成）；Unity 模拟页最终状态为故障且设置命令计数为一次，证明同批重复节点由最后一项覆盖，不会把首项缓存成增量或产生第二份三维设置。
- 同一回归随后只经外层 `view.open`（原子打开视图）完成“燃气 → 风电 → 燃气”。返回燃气后，模拟页设置命令总计为两次且最后状态仍为故障，证明壳保留最新完整快照并在物理场景重新激活后重投影；父页面没有为此重新发送节点状态。
- 本地模拟页仅由 `.env.local-shell-regression`（本地壳回归环境文件）中的受控参数拒绝 `offline`（离线）四态。回归发送离线完整快照后，父页面仍只收到一次二维 `completed`，模拟页失败计数为一次且不产生外层 `system.error`（系统错误）或第二份回执。这证明三维失败只进入内部受限诊断，不改变既有二维成功边界。
- 2026-08-14内置浏览器实测结果：父页按钮显示三项通过；模拟页受限诊断为设置 `2` 次、最后状态 `fault`（故障）、失败 `1` 次、活动动态状态 `1` 个；项目控制台错误和警告为零。该诊断只存在于 `tests/local-iframe-regression`（本地内嵌框架回归）模拟页，未进入生产构建或平台交付目录。
- 这三项补齐的是任务-014的本地三层证据，不代表合作方真实结构清单读取、平台内部绑定、真实燃气材质或真实网络失败。任务由75%更新为80%，不进入待验收。

## 2026-08-15 节点协议与全屏复验

- 内置浏览器确认节点双击只上报 `node.gas-turbine`，三维模拟页收到 `sceneNodeId + selectionId + isolate` 形式的 `focusNode`；三维反向选择携带 `sceneId + sceneNodeId + sceneActivationId`，前端按静态 `sceneNodeId → nodeId` 映射同步二维选择，不产生聚焦回写。
- 三维鼠标点击空白、地面或未映射对象时，Unity 在存在交互选择的情况下发送 `selectionCleared(sceneId, sceneActivationId)`；前端校验当前场景实例后清除二维节点和关联路径，不回发 `clearSelection`。
- 连续完整快照、状态边界、基础通信和单在途重试再次通过；三维受控失败仍不改变二维唯一成功回执。
- 本地宿主夹具已给协议壳内嵌框架补充标准原生全屏授权。三维和拓扑均可进入、退出原生全屏，往返过程中Unity内嵌框架始终为1个。
- 该证据只补齐本地协议、状态投影和平台容器权限模拟，不等同于合作方真实平台状态推送、真实燃气模型四态或实际平台全屏授权。

## 2026-08-15 启动遮罩与加载提示复验

- 嵌入壳在 Unity 创建、握手和首个稳定视图提交前使用覆盖三维与拓扑区域的不透明整区遮罩，完全挡住 Unity 内部品牌图和加载界面；运行时失败或释放时让出遮罩显示固定中文状态。
- 页面不再显示伪造的百分比、进度文字或进度条；Unity 进度事件仍保留在协调器内部，供控制台和联调排查使用，不进入页面内容。
- 浏览器实测切换期间遮罩背景为 `rgb(2, 6, 23)`、不透明度为 `1`，进度条节点数量为 `0`；首个稳定视图完成后遮罩节点数量为 `0`，控制台错误数量为 `0`。
- 该行为已写入[测试包与正式包输出标准](测试包与正式包输出标准.md)和联调包 `README.md` 模板；不改变三层协议、清单责任或本地夹具的合作方验收边界。

## 重新打开结论

## 2026-08-15 最新 Unity 场景联调包复核

- 旧开发地址 `http://127.0.0.1:4176/embed` 不作为正式联调入口；该地址可能命中旧开发服务和旧页面状态，缺少运行时同源直访参数时还会等待父页面初始化。
- 最新合作方联调包为 `Builds/Releases/gas-power-partner-runtime-origin-20260815-1615`，使用端口 `5575`，内含从当前 Unity 工程重新构建的 `task051-v5-latest-20260815-1615`。
- 新包冷启动复核的验收重点为：整区不透明遮罩覆盖 Unity 内部加载界面、无伪进度条和进度文字、首个稳定视图后解除遮罩、页面错误状态只显示中文提示；错误码和关联标识只留在控制台诊断。
- 本地三层夹具与合作方真实平台仍是两套验收边界；新包确认的是正式 Unity 资源和发布链路，不替代真实平台节点状态与来源验收。

现有夹具和来源、切换、失败恢复、超时、迟到与释放框架可复用，但尚未覆盖合作方最终契约，任务不能保持关闭。

## 未关闭项

1. 合作方真实结构清单地址、平台内部 `nodeId → 真实设备编号` 绑定变化和正式平台刷新时机仍未接入本地三层浏览器。
2. 合作方结构清单两类404、无缓存响应和正式接口来源仍待平台环境确认；旧第五百零一台绑定门禁不再属于我方前置条件。
3. 真实燃气模型四态、按 `nodeId` 的跨场景完整快照、真实三维失败与平台网络异常仍未完成端到端验收；本地夹具的对应受控机制不能替代上述正式证据。
4. 正式平台来源、反向代理、安全签字、平台容器全屏权限和回滚演练仍属于部署联调，不由本地夹具代替。

## 边界

1. 当前 Unity 为协议模拟页，不能替代正式 WebGL 构建、真实九场景资源、正式动作或三维反向选择验收；这些分别归属 020—022、037、039—047。
2. 合成清单仅用于协议闭集，不得作为任务-039—047的场景、设备、动作和资源资料。
3. 本夹具使用 600×600 专用尺寸阈值以适配内置浏览器通信测试窗口，不替代任务-002的产品最小尺寸验收；三维容器的实际 16:9 自适应布局已在浏览器中另行验证。
