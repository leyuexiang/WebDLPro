# 任务-040：燃气发电模板冻结与映射基线

> **2026-08-15 协议纠偏：** 0813布局、连线、过滤视图、三维聚焦、空白清除和总览复位继续冻结；`emit-device`、平台注入设备编号和运行时绑定清单不再是目标。任务-056已完成节点事件、状态和三维反查统一改用 `nodeId` 的本地迁移，任务-040保持80%。

## 当前状态

- 状态：进行中（80%）
- 场景键：`gas-power`
- Unity 场景路径：`Assets/Scenes/Business/GasPower.unity`
- 场景名称决策：保留 `GasPower`；用户已确认该现有场景即为燃气发电场景，名称与场景职责一致，不变更稳定场景键、资源标识或文件路径。
- 结论：现有场景可作为燃气发电模板的唯一基线；仅冻结已在代码、配置和用户确认中交叉证实的映射，不补造设备、对象或路由关系。现行视觉与二维/三维交互标准固定为 `Builds/Releases/燃气发电场景与拓扑图联动包0813`，包内发布标识为 `gas-power-platform-release-5550-20260813-2015`；更早的任务包和内部验证包只保留为分项证据。该包不是已完成的设备绑定或正式生产部署基线。
- 2026-08-08 协议复核：上述联调包属于旧网页协议版本。当前前端要求 Unity 在场景切换完成回执中携带 `sceneActivationId`（物理场景激活标识），旧网页图形基线未包含该字段；发布构建器已在复制资源前阻断此组合。燃气模板的场景、流程与聚焦源代码仍是当前基线，但新版网页图形构建完成前，旧包不能作为当前三层验收结论。
- 2026-08-14最新会议结论：燃气结构清单冻结资源内唯一 `nodeId → 可选 sceneNodeId` 和 `doubleClickBehavior`，不得出现 `deviceId`、`deviceMappings` 或绑定计数。下一版燃气总览23个源节点必须为 `emit-node`，三个过滤视图复用源节点行为和编号。平台读取 `nodeId` 后在平台内部绑定真实设备；二维提交决定外层成功，三维登记或下发失败只记录内部诊断，不得让外层整批失败。
- 2026-08-11 节点命名基线：九场景后续新增节点统一遵守[场景模型节点与拓扑节点命名规范](场景模型节点与拓扑节点命名规范.md)。燃气场景现有11个 `sceneNodeId` 作为已发布遗留接口冻结；其中 `node.hrsg.1`、`node.hrsg.2` 与 `hrsg` 存在目标集合重叠风险，模板关闭前必须明确可点击对象唯一回传的规范节点。
- 2026-08-13 历史测试基线：正式网页图形 `task063-unity-overall-20260813-0220` 与联调包 `gas-power-smoke-overview-action-final-5543-20260813` 只作为当日总览复位证据，使用23节点、22连线的CCGT运营技术总拓扑，并发布总览复位、燃气轮机、余热锅炉、蒸汽轮机四个受控动作及三个可单击聚焦的三维节点。总览动作不伪造二维节点映射，而是调用已验证的 `overview` 流程步骤；该包不再作为当前结构版本4协议基线。
- 2026-08-14 当前双基线：视觉与交互标准仍为 `Builds/Releases/燃气发电场景与拓扑图联动包0813`（包内 Unity 结构版本3、11项下行命令、燃气资源包122,913,353字节）；状态协议与平台联调基线为 `Builds/Releases/gas-power-platform-task038-v4-5551-20260813-2330`（包内 Unity 结构版本4、12项下行命令、燃气资源包122,952,961字节）。两者职责不同，后续记录不得把视觉包的结构版本或资源大小套用到状态协议包。
- 2026-08-13 资料对齐更新：新包 `gas-power-smoke-reference-filters-5545-20260813` 按《通用拓扑图参考0810_AI友好版》第3.1至3.3节展示集合生成三个关键环节，分别为16/13、13/6、15/10（节点/连线）。连线只筛选总图中两端均可见的已核验连接；余热锅炉6个、蒸汽轮机4个、燃气轮机2个资料规定的孤立现场节点使用 `allowedOrphanNodeIds` 显式登记，不创建虚假连线。总览仍为23/22，Unity映射仍复用现有三维场景节点，未新增设备绑定或三维目标。
- 2026-08-13 紧凑布局与路由冻结：总图五层纵坐标为 `8、28、50、69、88`；企业层和隔离区层横坐标均为 `24、50、76`，厂级层为 `14、38、62、86`，单元层为 `12、30、44、59、74、89`，现场层为 `6、18、30、44、59、74、89`。三个关键环节的 `nodeLayoutOverrides` 由发布构建器按 `nodeId` 从总图生成，只作为现有过滤类型的完整投影载体，不再维护独立拉伸坐标；燃气管网数据采集与监控网关保持在隔离区层。该调整只改变二维展示，不改变节点、连线、设备绑定、动作或三维映射。
- 2026-08-13 平台包入口隔离：中文0813交付目录中包内标识为 `gas-power-platform-release-5550-20260813-2015` 的版本只生成 `index.html`，目录内不存在 `self-test.html`；根入口只承载嵌入壳并自动初始化燃气总场景与23/22总拓扑。内部流程按钮、测试标题和状态诊断仅由构建器显式开关写入5549内部验证包，不能进入平台交付物。入口隔离不改变燃气场景、拓扑节点、动作清单或三维映射。

## 0813 联动包冻结标准

| 类别 | 冻结内容 | 后续回归要求 |
| --- | --- | --- |
| 拓扑数量 | 总览23/22、燃气轮机16/13、余热锅炉13/6、蒸汽轮机15/10 | 发布清单逐项锁定节点、连线及资料允许的孤立节点，不只比较总数 |
| 子图来源 | 三个关键环节都引用 `topology.gas-power.overview`，以完整 `nodeLayoutOverrides` 复用总图坐标 | 不得为子图建立第二份节点、连线、设备或三维映射事实 |
| 总图坐标 | 纵向 `8、28、50、69、88`；横向依次为 `24/50/76`、`14/38/62/86`、`12/30/44/59/74/89`、`6/18/30/44/59/74/89` | 任何布局调整都必须同时回归四张拓扑，不能使关键环节节点跳位 |
| 节点视觉 | 图标优先、文字在边框下方、常规图标约28—40像素、命中区至少40×40像素、不额外绘制状态圆点 | 缩放和平移后图标和文字仍可命中，重叠命中选择最近节点 |
| 连线路由 | 两端图标真实边界之间直线优先；冲突时依次选择一个或两个拐点的最短无碰撞路径，最后才回退通道 | 不得穿过第三方完整节点边界，不得共线重叠或产生非端点交叉 |
| 三维聚焦 | `inlet-duct → gas-turbine`、`hrsg → hrsg`、`steam-turbine → steam-turbine`，命令使用 `isolate: false` | 二维先选中自身及全部入/出边，三维失败不回滚；无映射节点只做二维选择 |
| 节点关联边界 | 0813 历史包的23个源节点实际为 `none`；当前源配置和新构建器已全部改为 `emit-node`，结构清单不含设备字段 | 过滤视图复用源节点 `nodeId`；只有已登记 `sceneNodeId` 的三个节点可派生三维目标，其余节点不猜测映射 |
| 空白清除 | 二维选择先清除，再异步发送 `clearSelection` | 只取消三维交互描边，镜头、流程、显隐和告警描边保持不变 |
| 总览复位 | `action.gas-power.overview` 进入 `gas-power-generation / overview` 后恢复23/22总图 | 三维成功前不得提前取消二维过滤；失败保持上一稳定上下文 |
| 实例与全屏 | 单三维实例、单拓扑画布；常规态同宽；两者均使用浏览器原生全屏 | 全屏往返和四图切换不得重建实例或丢失视图状态 |

该标准不包含平台内部节点绑定、平台真实状态到四态材质的端到端效果、三维路径或其余八个场景。任务-040完成度保持80%，直至取得完整节点状态快照、`sceneNodeId → nodeId` 唯一反向选择和合作方节点事件的正式证据。

## 证据与边界

| 对象 | 已证实事实 | 依据 | 可用于模板的范围 | 暂不允许推断的范围 |
| --- | --- | --- | --- | --- |
| Unity 燃气场景 | 现有业务场景对应燃气发电 | 用户确认；场景路径 `Assets/Scenes/Business/GasPower.unity` | 场景键、场景路径、加载入口 | 设备标识、模型对象名、二维节点到三维对象的对应关系 |
| 场景控制适配器 | 声明的场景键为 `gas-power` | `GasPowerBusinessSceneControllerAdapter.cs` | 初始化、步骤进入、节点聚焦、可见性、重置、释放 | 节点状态着色、三维路由流动；适配器会明确拒绝这两类动作 |
| 业务流程控制器 | 流程标识为 `gas-power-generation` | `PowerPlantProcessController.cs` | 流程步骤的选择与复位 | 未登记设备的状态控制 |
| 本地流程配置 | 总览页为 `gas-overview`，二维拓扑键为 `topology.gas-overview` | `local-process-config.ts` | 已有二维流程、拓扑、导览和详情内容的复用 | 将二维配置直接视为正式三维设备映射 |

## 已冻结的复用映射

| 模板字段 | 冻结值 | 证据 | 验证方式 |
| --- | --- | --- | --- |
| 业务场景键 | `gas-power` | 场景控制适配器常量 | 运行时场景目录与适配器注册校验 |
| Unity 场景 | `Assets/Scenes/Business/GasPower.unity` | 用户确认的既有场景迁移结果 | Unity 编辑器打开场景并确认可加载 |
| 流程标识 | `gas-power-generation` | 业务流程控制器与本地流程配置 | 流程控制器接受对应步骤 |
| 流程总览页 | `gas-overview` | 本地流程配置 | 前端加载本地流程配置时可找到总览页 |
| 二维拓扑键 | `topology.gas-overview` | 本地流程配置 | 前端拓扑读取与静态校验 |
| 二维导览步骤 | `overview`、`gas-network`、`inlet-duct`、`gas-turbine`、`hrsg`、`steam-turbine`、`generator`、`grid-output` | 本地流程配置 | 前端加载本地流程配置时可找到全部八项导览步骤 |
| 已发布三维流程步骤 | `overview`、`inlet-duct`、`gas-turbine`、`hrsg`、`steam-turbine`、`generator`、`grid-output` | `PowerPlantProcessController` 播放模式逐项回归；联调包生成器显式清单 | 生成清单的 `unitySceneMappings.processSteps` 恰为 7 项；每项播放模式命令成功 |
| 未发布三维流程步骤 | `gas-network` | 场景未登记独立燃气管网三维节点 | Unity 返回 `invalid-process-step`，清单不得声明该组合 |
| 已确认三维路由 | `route.exhaust-to-hrsg.1`、`route.exhaust-to-hrsg.2` | 本地流程配置的导览与场景映射 | 适配器收到路由动作时应保持拒绝，待真实三维路由能力实现后再改为正向验证 |

## 场景序列化控制器的直接证据

`GasPower.unity` 中的 `PowerPlantProcessController` 序列化字段 `_nodes` 已直接登记以下场景节点标识：

| 已登记场景节点标识 | 已登记目标数量 | 可确认结论 |
| --- | ---: | --- |
| `plant.overview` | 32 | 总览存在实际目标集合 |
| `unit.ccgt.1.gas-train`、`unit.ccgt.2.gas-train` | 各 13 | 两个机组的燃机训练目标集合已写入场景 |
| `node.hrsg.1`、`node.hrsg.2` | 各 13 | 两个余热锅炉节点目标集合已写入场景 |
| `inlet-duct`、`gas-turbine`、`hrsg`、`steam-turbine`、`generator`、`grid-output` | 各 1 | 六个流程节点已有直接场景目标 |

这些证据只说明“控制器已登记稳定场景节点到场景对象的关联”。场景中的目标多数来自预制体实例，YAML（Unity 文本序列化）显示为剥离引用，不能把文件编号、预制体名字或模型名字公开为设备标识。

## 流程节点登记复验

流程控制器现在会在修改可见性和镜头前校验全部场景节点。以下步骤已改为只引用 `GasPower.unity` 序列化 `_nodes` 中已有的稳定标识；机组分组仍只决定可见范围，不再按机组号拼接未登记的聚焦标识。

| 流程分支 | 当前可见/聚焦标识 | 当前结论 |
| --- | --- | --- |
| `overview` | `plant.overview` | 已登记并通过播放模式回归 |
| `inlet-duct` | 已登记燃机分组 + `inlet-duct` | 已登记并通过播放模式回归 |
| `gas-turbine`、`hrsg` | 已登记燃机分组 + 对应直接流程节点 | 已登记并通过播放模式回归 |
| `steam-turbine` | `steam-turbine` | 旧 `steam-train` 分组未登记，已改用直接流程节点并通过回归 |
| `generator`、`grid-output` | 已登记燃机分组、`steam-turbine`、`generator`、`grid-output` | 旧 `utility.grid`、`node.grid` 与未登记汽机分组已移除，当前通过回归 |
| `gas-network` | 无 | 场景没有独立燃气管网稳定节点，明确返回 `invalid-process-step`；不得借用进气、总览或模型名伪造映射 |

该处理只修复场景内已登记节点的流程控制一致性；它不确认双机组设备语义、外部设备标识或二维设备映射。

## 能力边界

| 动作类别 | 当前结论 | 依据 | 模板处理方式 |
| --- | --- | --- | --- |
| 初始化 | 已支持 | 场景控制适配器能力声明 | 保留 |
| 进入流程步骤 | 已支持 | 场景控制适配器能力声明 | 保留 |
| 聚焦节点 | 已支持 | 场景控制适配器能力声明 | 仅能传递已有流程节点标识 |
| 节点可见性 | 已支持 | 场景控制适配器能力声明 | 仅能传递已有流程节点标识 |
| 重置与释放 | 已支持 | 场景控制适配器能力声明 | 保留 |
| 节点状态视觉更新 | 有条件支持，属于未关闭联调项 | 燃气适配器仅在三个真实模型完成四态登记后声明更新与清除能力；资产级编辑器测试已验证该登记前置条件 | 状态项 `nodeId` 对应的结构节点存在 `sceneNodeId` 时必须发起同步尝试；尚缺真实平台节点状态到材质及基础视觉恢复的端到端验收，任何内部失败均不影响二维外层成功 |
| 三维路由流动 | 未支持 | 适配器对路由动作返回“不支持” | 不写入模板必测项；已确认的二维路由仍保留为待接入证据 |

## 仍缺失的正式映射

| 缺失项 | 风险 | 后续录入依据 | 验证方式 |
| --- | --- | --- | --- |
| 下一包的节点双击与平台内部绑定 | 0813历史包全部为 `none`，无法按最新协议上报节点编号 | 当前构建器和源配置已改为 `emit-node` 与单结构清单；0813只作为视觉基线 | 生成新的合作方包，确认过滤视图复用 `nodeId`，逐项验证双击只上报节点编号且平台内部能查到真实设备 |
| 二维节点到场景对象 | 不能把流程名称当作对象名 | 已确认的节点、热点和对象清单 | 前端节点点击与 Unity 聚焦回归 |
| 三维路由到对象路径 | 介质、方向或对象错误会造成误导 | 实际管线对象、方向和介质确认记录 | 路由动作正向回归与目视核对 |
| 节点状态语义 | 三个真实模型的四态登记前置条件已通过；平台真实状态效果未验收 | 按 `nodeId` 的状态协议、结构清单及已登记材质/对象实现 | 平台状态变更前后截图对比，并验证缺失节点恢复基础视觉 |
| 可见性作用域 | 隐藏错误对象会破坏场景 | 场景层级与流程节点映射 | 重置后对象完整性回归 |
| 三层验收记录 | 缺少可追溯性会让后续八个场景模板失真 | 前端、场景、业务三方确认 | 映射清单审阅并纳入发布校验 |
| 燃气管网独立节点 | 当前没有可供 `gas-network` 使用的三维稳定节点；错误映射会误导操作人员 | Unity 场景负责人确认的节点登记与流程定义 | 补充登记后将播放模式中的明确拒绝改为正向聚焦与可见性回归 |

## 本轮验证

- Unity 编辑器模式：历史映射基线为19 / 19通过，结果见 `artifacts/task040-editmode-results.xml`；当前四态资产专项为23 / 23通过、0失败、0跳过，结果见 `artifacts/unity-tests/editmode-task038-real-state-fixed-20260814-115922.xml`。后者只证明真实模型登记前置条件，不替代平台状态端到端验收。
- Unity 播放模式：6 / 6 通过，结果见 `artifacts/task040-playmode-final-results.xml`。其中 7 个已登记流程步骤逐项成功，`gas-network` 明确拒绝且不产生替代聚焦。
- 正式 WebGL 构建：`task040-process-nodes-20260805-1842` 成功，构建日志见 `artifacts/task040-unity-webgl-build.log`。
- 最终燃气联调包：`gas-power-smoke-task040-process-nodes-5524-20260805-1855` 通过清单契约校验与前端类型检查、生产构建；内部浏览器在 `http://127.0.0.1:5524/` 实际完成三层初始化，显示 24 个拓扑节点、23 条连线和有效 Unity 三维入口；用户已在该测试包人工确认“点击拓扑节点后，Unity 三维模型聚焦并描边”。该确认只证明已发布的正向聚焦链路可用，不推导设备标识、燃气管网节点或其余八个场景映射。
- 当前发布清单基线：联调包生成器将上述 7 项流程组合写入 `unitySceneMappings.processSteps`，但不创建任何宿主流程动作；`gas-network` 继续不发布。最新包 `gas-power-smoke-mapping-summary-5527-20260805-2015` 将最终清单派生的映射摘要写入 `release-manifest.json`，逐字段复核为版本 `2026.08.01-local.2`、7 项流程、6 个三维节点、0 条三维路径，契约报告为 0 项问题，内部浏览器完成三层初始化；不能因该声明反推设备、路径或状态能力。
- 2026-08-13 历史正式复验：`task061-webgl-switch-ack-fixed-20260813-0036` 已生成结构版本2协议元数据；应用内浏览器与独立浏览器均呈现真实燃气模型和23节点、22连线总图。资料对齐包中的三个流程分别显示16/13、13/6、15/10（节点/连线）并保持同一个Unity内嵌框架；蒸汽轮机流程截图可见模型聚焦和描边，证据为 `artifacts/ccgt-task061-steam-turbine-browser-20260813.png`。
- 2026-08-13 历史总览闭环复验：`task063-unity-overall-20260813-0220` 的Unity编辑器模式22 / 22、播放模式11 / 11通过。联调包清单契约为0项问题，明确包含 `action.gas-power.overview` 和 `gas-power-generation / overview`；浏览器依次进入三个关键环节后返回总览，Unity恢复整厂镜头并清除流程描边，二维恢复23节点、22连线，单Unity内嵌框架和单拓扑画布不变。独立回归截图为 `artifacts/browser-tests/overview-action-final-5543-20260813.png`。
- 2026-08-13 历史清除交互描边复验：拓扑空白点击独立发送 `clearSelection`，燃气控制器只执行 `ClearProcessHighlight()`，不调用场景复位。前端全量230项、Unity编辑器模式22 / 22、播放模式11 / 11通过；结构版本3正式Unity包 `task064-clear-selection-20260813-1610` 已声明完整命令能力。内置浏览器在 `gas-power-smoke-clear-selection-5544-20260813` 中确认模型聚焦、描边清除和上下文保持。
- 2026-08-13 历史紧凑直线路由复验：不可变发布目录 `Builds/Releases/gas-power-smoke-topology-compact-straight-final-5548-20260813-191145` 复用 `task064-clear-selection-20260813-1610`，未重新构建 Unity。前端40个测试文件、241项、类型检查、生产构建和发布清单校验全部通过；总览及三个关键环节数量锁定为23/22、16/13、13/6、15/10。该证据不包含设备动态绑定、状态快照、其余八个场景或新增三维能力，因此完成度保持80%。
- 平台交付包复验：`Builds/Releases/燃气发电场景与拓扑图联动包0813` 复用同一Unity基线，内部发布标识为 `gas-power-platform-release-5550-20260813-2015`，未重建Unity。目录检查确认 `self-test.html` 不存在、`release-manifest.json` 中 `selfTestIncluded` 为 `false`、根入口无测试控件；内置浏览器确认真实燃气模型、23/22总拓扑、单Unity实例和单拓扑画布正常。内部5549包继续承担四个动作的自动回归。前端全量40个测试文件、243项通过；本次仅收紧交付边界，完成度保持80%。
- 2026-08-14 当前结构版本4协议复验：`Builds/Releases/gas-power-platform-task038-v4-5551-20260813-2330` 复用 `task038-state-clear-v4-20260813-232422`，元数据结构版本4、12项下行命令、节点视觉状态结构版本3和动态状态清除结构版本1均通过发布门禁；燃气资源包为122,952,961字节。该包只证明状态协议构建、平台根入口和现有燃气视觉前置可加载，不替代合作方动态设备清单、真实状态到模型材质的端到端验收。

## 冻结规则

1. 不根据名称、位置或拓扑连线猜测节点、真实设备、Unity 对象名和路由对象；我方结构清单不得包含 `deviceId`。
2. 二维已验证关系不自动升级为三维已验证关系。
3. 适配器明确拒绝的能力不得伪装成可用能力；待实现后必须补充正向回归。
4. 后续八个业务场景只复用本文件已冻结的字段结构与验证规则，不复用燃气场景中的未证实实体映射。
5. 正式结构映射进入清单前，必须同时保留来源、确认人、验证日期和回归结果；平台内部绑定变化不改写我方清单，必要时由父页面重新初始化壳。
6. 未登记流程步骤必须返回受控失败；不得因为旧页面可点击、文本相近或模型看起来相邻而改写为其他三维节点。
7. 后续新增节点必须遵守统一命名规范；已发布节点确需改名时，Unity 登记、基础拓扑、动作引用、清单版本和场景映射版本必须在同一发布批次原子升级。
