# 场景拓扑动作映射规范

**适用范围：** 九个 Unity 业务场景、全部二维拓扑、外部菜单和子项动作。

**目标：** 让外部业务动作、Unity 场景操作、拓扑切换和设备标识通过稳定配置关联，禁止在组件中硬编码或根据名称猜测。

---

## 一、核心原则

1. 外部父页面只发送 `sceneId`、`topologyId` 或 `actionId`，不发送 Unity 方法名、场景文件路径、模型名称和层级路径。
2. 当前子应用根据受版本控制的动作定义解析目标场景、目标拓扑和 Unity 命令。
3. 场景、拓扑、动作、设备和 Unity 映射必须随同一个清单版本原子发布。
4. 未登记、版本不一致或存在孤立引用时禁止发布，不能在运行时按中文标题或相似字符串推断。
5. 同一动作的场景与拓扑变化属于一个事务，只允许一起成功提交或一起失败。

---

## 二、标识职责

| 标识 | 事实来源 | 用途 |
| --- | --- | --- |
| `sceneId` | 九场景固定目录 | 当前 Unity 业务场景 |
| `topologyId` | 拓扑交付清单 | 当前二维拓扑 |
| `actionId` | 外部前端与可视化团队共同确认 | 父页面菜单、按钮或子项动作 |
| `nodeId` | 单套拓扑定义 | 二维节点选择与绘制 |
| `deviceId` | 外部业务系统设备主数据 | 拓扑双击对外上报和设备状态关联 |
| `sceneNodeId` | Unity 场景映射表 | 三维聚焦、状态和对象选择 |
| `processId`、`stepId`、`routeId` | Unity 场景流程配置 | 受控流程和路径命令 |

`nodeId`、`deviceId` 和 `sceneNodeId` 不能默认视为同一个值。即使当前燃气场景暂时相同，也必须通过映射字段显式关联。

---

## 三、九场景目录

| 场景名称 | `sceneId` | Unity 场景键 | 默认拓扑 | 状态 |
| --- | --- | --- | --- | --- |
| 燃煤发电 | `coal-power` | 待场景交付后登记 | 待确认 | 待配置 |
| 燃气发电 | `gas-power` | 依据现有燃气场景登记 | 待从现有拓扑正式命名 | 基线可复用 |
| 风力发电 | `wind-power` | 待场景交付后登记 | 待确认 | 待配置 |
| 光伏发电 | `solar-power` | 待场景交付后登记 | 待确认 | 待配置 |
| 变电站 | `substation` | 待场景交付后登记 | 待确认 | 待配置 |
| 配电网 | `distribution` | 待场景交付后登记 | 待确认 | 待配置 |
| 用能侧 | `consumption` | 待场景交付后登记 | 待确认 | 待配置 |
| 微电网 | `microgrid` | 待场景交付后登记 | 待确认 | 待配置 |
| 调度运行 | `dispatch` | 待场景交付后登记 | 待确认 | 待配置 |

本表不猜测 Unity 场景文件名和正式拓扑标识。场景制作负责人必须随交付物提供正式映射。

---

## 四、配置模型

### 4.1 场景定义

```ts
/** 场景定义只登记稳定业务标识和受控资源键，不暴露文件系统路径。 */
interface SceneDefinition {
  sceneId: SceneId
  title: string
  unitySceneKey: string
  defaultTopologyId: string
  topologyIds: readonly string[]
  supportedActionIds: readonly string[]
  sceneMappingVersion: string
  resourceVersion: string
  switchStrategy: 'unload-first' | 'preload-then-unload'
}
```

`preload-then-unload` 仅允许内存压测通过的轻量场景使用；默认必须为 `unload-first`。

### 4.2 拓扑定义

```ts
/** 一套拓扑只能属于一个场景，但同一设备可出现在该场景的多套拓扑中。 */
interface TopologyDefinition {
  topologyId: string
  sceneId: SceneId
  title: string
  configVersion: string
  nodes: readonly TopologyNodeDefinition[]
  edges: readonly TopologyEdgeDefinition[]
}

/** 三类标识分别服务于二维、外部业务和三维，不允许隐式互换。 */
interface TopologyNodeDefinition {
  nodeId: string
  title: string
  deviceId?: string
  sceneNodeId?: string
  deviceType?: string
  iconKey: string
  x: number
  y: number
  deviceStatus: 'normal' | 'alarm' | 'fault' | 'offline'
  doubleClickBehavior: 'emit-device' | 'none'
}
```

`doubleClickBehavior: 'emit-device'` 的节点必须有 `deviceId`；概念分组、层级标题和非设备节点必须使用 `none`。

### 4.3 动作定义

```ts
/**
 * actionId 是父页面唯一需要理解的动作标识。
 * targetSceneId、targetTopologyId 和 unityAction 由发布清单控制。
 */
interface ActionDefinition {
  actionId: string
  title: string
  targetSceneId: SceneId
  targetTopologyId: string
  allowedParameters: readonly string[]
  unityAction: UnityActionDefinition
  failurePolicy: 'keep-current-context' | 'commit-view-with-warning'
}

type UnityActionDefinition =
  | { type: 'none' }
  | {
      type: 'enterProcessStep'
      processId: string
      stepId: string
      defaultUnitId?: string
      isolate: boolean
    }
  | {
      type: 'focusNode'
      sceneNodeId: string
      isolate: boolean
    }
  | { type: 'resetScene' }
  | {
      type: 'setRouteFlow'
      routeId: string
      enabled: boolean
    }
```

`commit-view-with-warning` 只适用于不影响场景正确性的非关键展示动作。流程步骤、场景加载和关键聚焦默认使用 `keep-current-context`。

### 4.4 设备映射

```ts
/** 一个设备可出现在多套拓扑中，但当前场景内最多映射到一个活动三维节点。 */
interface DeviceMappingDefinition {
  deviceId: string
  sceneId: SceneId
  topologyNodeRefs: readonly {
    topologyId: string
    nodeId: string
  }[]
  sceneNodeId?: string
}
```

---

## 五、动作示例

以下示例只说明格式，不代表正式业务映射已经确认。

### 5.1 同场景子项

```json
{
  "actionId": "gas-power.turbine",
  "title": "燃气轮机子项",
  "targetSceneId": "gas-power",
  "targetTopologyId": "gas-power.turbine-control",
  "allowedParameters": ["unitId"],
  "unityAction": {
    "type": "enterProcessStep",
    "processId": "gas-power-generation",
    "stepId": "gas-turbine",
    "defaultUnitId": "unit-01",
    "isolate": true
  },
  "failurePolicy": "keep-current-context"
}
```

当前已经处于 `gas-power` 时，协调器不切换 Unity 场景，只触发流程并切换目标拓扑。

### 5.2 跨场景入口

```json
{
  "actionId": "wind-power.overview",
  "title": "进入风力发电",
  "targetSceneId": "wind-power",
  "targetTopologyId": "wind-power.overview",
  "allowedParameters": [],
  "unityAction": {
    "type": "resetScene"
  },
  "failurePolicy": "keep-current-context"
}
```

当前场景不是 `wind-power` 时，协调器先切换 Unity 场景，场景就绪后再执行复位动作并激活目标拓扑。

---

## 六、解析与执行规则

### 6.1 `view.open`

1. 校验 `sceneId` 存在。
2. 校验 `topologyId` 属于该场景。
3. 有 `actionId` 时校验动作目标与场景、拓扑一致。
4. 创建新的 `transitionId`。
5. 预解析目标拓扑。
6. 场景不同则切换 Unity 场景；场景相同则复用当前场景。
7. 执行可选 Unity 动作。
8. 激活目标拓扑并提交上下文。

### 6.2 `workflow.trigger`

1. 按 `actionId` 获取动作定义。
2. 拒绝未在 `allowedParameters` 中声明的参数。
3. 将允许参数与动作默认值合并，外部参数不能覆盖 `processId`、`stepId`、`sceneNodeId` 和 `routeId`。
4. 按目标场景是否变化决定是否发送 `switchScene`。
5. 执行映射后的 Unity 动作。
6. 激活映射拓扑。
7. 成功后递增 `contextRevision`。

### 6.3 拓扑单击

1. 立即更新当前 `nodeId` 选择和路径高亮。
2. 有 `sceneNodeId` 时发送受控 `focusNode`。
3. 没有三维映射时只保留二维选择，不猜测模型。
4. 来源为 Unity 反向选择时不再次发送 `focusNode`。

### 6.4 拓扑双击

1. 复用单击后的选中结果。
2. `doubleClickBehavior` 必须为 `emit-device`。
3. `deviceId` 必须存在。
4. 向父页面发送 `topology.node.dblclick`。
5. 不重复发送相同三维聚焦命令。

---

## 七、发布校验规则

### 7.1 场景校验

- 九个固定 `sceneId` 必须齐全且唯一。
- 每个场景必须有 `unitySceneKey`、默认拓扑、拓扑清单、映射版本和资源版本。
- 默认拓扑必须属于当前场景。
- `supportedActionIds` 中每个动作必须存在且目标场景一致。

### 7.2 拓扑校验

- `topologyId` 全局唯一。
- 每个 `nodeId` 在所属拓扑内唯一。
- 每条边的起终节点必须存在。
- 可双击设备节点必须有 `deviceId`。
- 可三维聚焦节点必须有 `sceneNodeId`。
- 同一场景同一 `deviceId` 的多个节点必须显式登记，不依赖标题匹配。

### 7.3 动作校验

- `actionId` 全局唯一。
- 目标场景和目标拓扑必须存在且相互匹配。
- Unity 动作引用的流程、步骤、节点和路径必须存在于目标场景映射版本。
- 外部允许参数必须为稳定业务范围参数，禁止颜色、材质、脚本名和资源路径。
- 关键动作不得使用会在失败后提交错配视图的失败策略。

### 7.4 版本校验

以下版本必须作为一个发布事务提交：

- 场景拓扑清单版本。
- Unity 构建标识。
- Unity 场景映射版本。
- 拓扑配置版本。
- 动作映射版本。
- 设备映射版本。

任一版本缺失或不一致时，父页面初始化必须失败，不能拼接旧缓存继续运行。

---

## 八、每个场景的交付模板

九个场景负责人必须分别提交：

```text
场景名称：
sceneId：
Unity 场景键：
场景映射版本：
资源版本：
默认拓扑：
可切换拓扑清单：
动作清单：
设备映射表：
流程步骤映射表：
路径映射表：
加载策略：
传输体积：
峰值内存：
失败和降级策略：
负责人：
验收日期：
```

没有完整填写的场景不得加入生产清单。

---

## 九、禁止项

1. 禁止用中文标题、数组顺序、图片名称或坐标邻近关系推断映射。
2. 禁止外部父页面直接下发 Unity 方法名和任意参数。
3. 禁止在 Vue 组件中用 `if (sceneId === ...)` 硬编码九场景流程。
4. 禁止一个动作分别更新场景和拓扑而不经过统一事务。
5. 禁止设备节点缺少 `deviceId` 却发送双击设备事件。
6. 禁止用 Unity 层级路径作为 `sceneNodeId`。
7. 禁止跨版本混用拓扑、动作和 Unity 映射缓存。
8. 禁止为每套拓扑或每个场景创建隐藏内嵌框架、隐藏画布或常驻渲染实例。
