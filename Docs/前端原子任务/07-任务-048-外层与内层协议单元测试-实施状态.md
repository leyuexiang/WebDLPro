# 任务-048：外层与内层协议单元测试实施状态

**状态：** 已完成（100%）
**更新时间：** 2026-08-14

## 合作方契约变更复核

- 重复 `messageId` 的新规则是直接丢弃且不发送第二份业务回执；现有“完成结果重放或返回重复错误”的测试与实现需要调整。
- `system.ready` 必须在页面加载后 15 秒内、且运行时注入清单、唯一拓扑运行时和 Unity 运行时均完成准备后发送；超时必须发送一次脱敏 `system.error` 并禁止迟到 `system.ready`；`system.init` 失败必须同时发送失败 `system.ack` 和关联 `system.error`。
- `device.states.update` 必须校验必填但仅用于诊断的 `sourceRevision`、带时区状态时间、`nodeId` 格式和完整快照载荷；三维尽力同步失败不得产生外层失败结果或外层错误。
- 当前源码已覆盖500/501消息容量、同批最后一项、较小来源修订仍提交、本地快照序号迟到隔离、三维失败不产生第二份外层结果、重复标识静默丢弃、15秒总就绪期限和初始化失败双回执。完整命令集的协议边界已由专项测试重新验证；真实嵌入壳、合作方结构清单和平台内部绑定属于任务-052端到端验收，不再作为本任务关闭条件。

## 验收证据

| 任务验收项 | 直接测试证据 | 结论 |
| --- | --- | --- |
| 两层信封与逐类型载荷 | `host-protocol.spec.ts` 覆盖六类外层命令、八类外层事件、字段缺失、错误通道、版本、标识和超大消息；`protocol.spec.ts` 覆盖内层就绪、确认、场景切换、流程、聚焦、四态、路径、显隐、复位、对象选择和三维空白清除载荷 | 已覆盖 |
| 两个通道完全隔离 | `message-router.spec.ts` 分别分发 `power-scene-topology-shell`（外层场景拓扑通道）与 `power3d-unity`（Unity 网页图形通道），未知通道只留 50 条有限诊断 | 已覆盖 |
| 来源、窗口、实例、会话 | `host-bridge.spec.ts` 覆盖伪造来源、同源其他窗口、错误实例和旧会话；`runtime-connector.spec.ts` 覆盖内层错误来源、窗口和实例 | 已覆盖 |
| 能力、确认与原请求关联 | `host-handshake.spec.ts`、`host-command-dispatcher.spec.ts`、`host-event-sender.spec.ts`、`host-runtime-composition.spec.ts` 覆盖握手、能力声明、`replyTo`（回复关联）和逐命令回包；`runtime-connector.spec.ts` 覆盖内层请求确认与场景最终回执 | 已覆盖 |
| 去重、超时、容量与释放 | 当前专项覆盖500项成功、501项整体拒绝、同批最后一项、本地快照序号迟到隔离、三维失败隔离、重复标识静默丢弃、初始化失败双回执；新增页面级15秒期限测试覆盖成功清理、超时单发、迟到成功不可恢复和释放清理 | 已覆盖 |
| 错误来源、旧会话、未知类型与超大载荷不得进入协调器 | 外层桥在分派前校验来源、窗口、实例和会话；内层连接器在回调前校验来源、窗口、实例和事件能力；`host-protocol.spec.ts` 对未知命令和超过 256 KiB 的消息断言稳定拒绝 | 已覆盖 |

## 验证结果

- 2026-08-14 协议专项重新执行：`embedded-shell-startup-deadline.spec.ts`、外层握手、组合根、桥接、载荷、事件、命令生命周期及内层连接器共8个文件、66项通过；覆盖页面级15秒期限、超时单发、迟到隔离、启动错误脱敏、原请求关联与重复消息静默。
- 当前选择清除专项覆盖 `selectionCleared` 的严格两字段校验、场景实例门禁、二维空选择提交、关联路径清除和不回发 Unity 命令；三维选择与清除相关专项共3个文件、29项通过。
- 同轮全量前端单元测试为41个文件、267项通过；类型检查通过；生产构建通过并转换96个模块。
- 内置浏览器对结构版本4正式平台包 `gas-power-platform-task038-v4-5551-20260813-2330` 的根入口复核显示：真实Unity内嵌框架与燃气总图 `23` 节点、`22` 连线均正常加载。该浏览器结果只佐证已发布壳可加载，不代替任务-052要求的平台动态清单、状态快照和失败链路端到端验证。

## 当前结论

既有协议安全、容量和释放测试已复用。当前专项确认：`nodeId` 和带时区状态时间校验、来源修订仅作诊断、同批最后一项、500项通过、501项无部分提交、本地快照序号替代来源修订排序、三维失败不产生第二份外层结果、重复标识静默丢弃、页面级15秒就绪期限及初始化失败双回执。任务-048的协议测试交付与验收项均已有直接自动化证据，现关闭为100%。正式场景映射、真实网页图形构建、合作方结构清单读取和完整三层浏览器端到端结果仍分别由任务-039—047、051、052验收，不随本任务关闭而改变。
