# 任务-036：快速切换取代与失败恢复实施状态

## 当前状态

- 状态：进行中（70%）
- 已完成：协调器的新事务提交权取代、旧回调过滤、连续三次切换只提交最后一次、动作阶段迟到结果过滤、上一稳定上下文的物理三维与单画布回退，以及回退失败后的明确错误视图。
- 未完成：真实 Unity 网页图形构建的资源取消确认，以及具有正式动作映射的多场景恢复验收。

## 已验证行为

1. 连续三条 `view.open` 在前两条 Unity 等待期间进入时，只有第三条可以激活画布并提交稳定上下文；前两条均返回 `superseded`。
2. 场景切换与动作等待阶段的迟到成功、失败回调都会先验证活动事务，旧回调不能激活旧拓扑、发送稳定视图或覆盖最后上下文。
3. 目标场景已经切换但动作或拓扑阶段失败时，处理器会用上一个已验证场景映射回切 Unity，并恢复单画布的上一稳定拓扑后才恢复状态仓库。
4. 回退场景、拓扑或既有动作状态无法由正式清单安全证明时，稳定上下文与选择会被清空，运行时进入明确错误态，嵌入壳停止双容器交互并提供重新初始化入口。

## 验证位置

- `power-data-web/src/modules/visual/orchestration/view-open-transaction-handler.spec.ts`
- `power-data-web/src/modules/visual/orchestration/visualization.store.spec.ts`
- 本轮前端全量回归：28 个测试文件、111 项通过；类型检查与生产构建通过。
- 内部浏览器：正式嵌入壳三层夹具已通过连续三次外层 `view.open` 取代、最终失败恢复、十秒超时、迟到回调隔离与释放确认；所有断言只读取外层命令结果与有限状态快照。

## 尚未关闭的事项

1. 当前浏览器验证使用本地 Unity 模拟页；真实 Unity 场景资源尚未在浏览器完成，不能确认旧事务在卸载、下载和动作阶段的实际资源取消与释放。
2. 任务-034、035尚未取得正式动作映射和失败策略，带有既有动作状态的上下文不会被猜测性重放，只能进入明确错误态等待重新初始化。
