# 任务-020：单发布版本与场景资源拆分实施状态

**状态：** 进行中（70%）  
**更新时间：** 2026-08-04

## 已实施

- 保持一个 Unity WebGL 入口。正式构建仅把 Bootstrap 放进主播放器，九个业务场景改由同级 SceneBundles 目录提供，不再全部进入首屏数据文件。
- 新增场景资源构建脚本。它只读取 BusinessSceneCatalog 已登记的九项映射，构建九个独立场景资产包；不扫描模型名称或以文件名猜测业务归属。
- 构建时递归读取 Unity 依赖。被两个及以上场景引用的可打包资产单独进入 scene-shared，由 Unity 清单声明依赖，避免同一公共材质、着色器或图集被重复打包。
- 每个发布目录均生成 SceneBundles/scene-catalog.json 与 SceneBundles/scene-content-summary.json。前者包含发布标识、场景包、哈希、循环冗余校验、依赖图和稳定场景标识；后者列出独占与共享依赖，供发布核验和回滚追溯。
- 新增运行时资产包加载器。发布 WebGL 从同级目录下载受限清单，以哈希和循环冗余校验加载依赖及场景包，复用 Unity 缓存；编辑器仍按正式目录直接加载，避免人工编辑和编辑器测试依赖已发布包。
- 已修复资源包场景加载编译错误。资源包只负责下载、内容校验和依赖租约，场景一律由 Unity 的 `SceneManager.LoadSceneAsync` 按已核验路径加载；编辑器于 2026-08-04 完成强制同步重编译，未再出现 `AssetBundle.LoadSceneAsync` 的 CS1061 错误，并新增静态防回归断言。
- 加载器在开始下载时即登记临时资源租约。成功、失败、事务取代、场景卸载和运行壳释放均经由同一租约释放内存资产包句柄，不清空版本化磁盘缓存。
- 正式构建使用 Builds/Releases/<发布标识>/unity；已存在的正式发布目录会被拒绝覆盖，构建只在唯一暂存目录完成后整体移动，旧版本可作为明确回滚目标。发布标识可通过命令行 -webglReleaseId 提供，缺省使用项目版本。
- 构建前先生成资产包清单，并传入主播放器构建的资产包清单路径，防止代码裁剪移除仅被业务场景包引用的控制器类型。

## 资料边界

- 当前仅燃气发电场景含用户已有的真实业务内容；其余八个场景为明确的空场景占位。构建脚本会建立完整九包结构，但不能把空包误报为八个正式业务资源包。
- 当前未安装可寻址资源系统。已使用工程已有的 Unity 资产包模块；在缺少版本、缓存、回滚和离线失败方案前，不引入可寻址资源系统。
- 正式场景资源所有者、容量基线和单场景内容验收仍由任务-039—047提供；任务-020不改动燃气场景内部资产，也不猜测其余场景模型。

## 尚待验证

1. 当前交互式 Unity 编辑器持有项目锁，尚未运行编辑器模式、运行模式或实际 WebGL 构建；不会终止用户进程。
2. 项目锁释放后，需构建一个开发包和一个带新发布标识的正式包，检查主 Build 数据不含九个业务场景、SceneBundles 恰含九个场景包及可选共享包，并校验两个 JSON 文件。
3. 需在浏览器中连续切换燃气与占位场景，确认首次下载、同版本缓存命中、失败时的固定错误码、事务取代释放和正式目录回滚。
4. 待九个正式资源交付后，对内容摘要的共享依赖进行复核，并以真实包尺寸、缓存命中和内存峰值替换当前结构性验证。

## 涉及文件

- Assets/Editor/PowerPlantSceneBundleBuild.cs
- Assets/Editor/PowerPlantWebGlBuild.cs
- Assets/Scripts/Visualization/Scenes/SceneBundleRuntimeLoader.cs
- Assets/Scripts/Visualization/Scenes/MultiSceneCoordinator.cs
- Assets/Editor/BusinessSceneBootstrapGenerator.cs
